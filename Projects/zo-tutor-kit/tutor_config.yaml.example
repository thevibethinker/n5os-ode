# Example configuration for the Zo Tutor Kit
# Copy or generate this as `tutor_config.yaml` via `tutor_cli.py init`.

zo_id: example.zo
config_version: 0.1

# Future fields (not yet used):
# - default_ttl_seconds: 600
# - log_root: /home/workspace/Logs/tutor_sessions
# - allowed_modes: [capability, knowledge, data]

