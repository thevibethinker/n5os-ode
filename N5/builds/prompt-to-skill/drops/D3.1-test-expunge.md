---
drop_id: D3.1
build_slug: prompt-to-skill
stream: 3
depends_on: ["D2.1", "D2.2", "D2.3"]
spawn_mode: manual
status: pending
thread_title: "[prompt-to-skill] D3.1: Integration Tests + Expunge"
---

# D3.1: Integration Tests + Expunge

## Mission

Verify all skills work correctly, test fail-safes, and expunge the original prompt.

**This is MANUAL spawn** — requires human judgment for testing and expunge decisions.

## MUST DO

### Phase 1: Integration Tests

#### Test 1: thread-close

```bash
# Should work (normal thread)
python3 Skills/thread-close/scripts/close.py --convo-id con_plquQK5mpVEUO74p --dry-run

# Expected: "Running thread-close (Tier N) for con_..."
```

#### Test 2: drop-close guard

```bash
# Should WARN (current thread is not a drop)
python3 Skills/drop-close/scripts/close.py --convo-id con_plquQK5mpVEUO74p

# Expected: "⚠️ WRONG SKILL DETECTED ... Suggested: thread-close"
```

#### Test 3: build-close guard

```bash
# Should WARN (build not terminal)
python3 Skills/build-close/scripts/close.py --slug prompt-to-skill

# Expected: "⚠️ WRONG SKILL DETECTED ... Build not finished"
```

#### Test 4: build-close with force

```bash
# Should work with --force (bypasses guard)
python3 Skills/build-close/scripts/close.py --slug prompt-to-skill --dry-run --force

# Expected: "Running build-close for prompt-to-skill ... [DRY RUN]"
```

#### Test 5: prompt-to-skill assess

```bash
# Should score the original prompt
python3 Skills/prompt-to-skill/scripts/assess.py "Prompts/Close Conversation.prompt.md"

# Expected: Score >= 20, Recommendation: CONVERT
```

#### Test 6: Import validation

```bash
# All imports should work
python3 -c "from N5.lib.close import guards, core, emoji, positions, content_library, aar, pii; print('✓ All imports work')"
```

### Phase 2: Find References to Old Prompt

```bash
# Find references to old prompt
grep -r "Close Conversation" /home/workspace --include="*.md" --include="*.py" --include="*.json" 2>/dev/null | grep -v "Archive" | grep -v ".z/workspaces"

# Find references to old scripts
grep -r "conversation_end_" /home/workspace --include="*.md" --include="*.py" 2>/dev/null | grep -v "N5/lib/close"
```

Document what references exist and whether they need updating.

### Phase 3: Expunge Original Prompt

#### Step 1: Archive the prompt

```bash
mkdir -p /home/workspace/Prompts/Archive
mv "/home/workspace/Prompts/Close Conversation.prompt.md" "/home/workspace/Prompts/Archive/Close Conversation.prompt.md"
```

#### Step 2: Create redirect stub

```bash
cat > "/home/workspace/Prompts/Close Conversation.prompt.md" << 'EOF'
---
title: Close Conversation (DEPRECATED)
deprecated: true
redirect_to: Skills/thread-close/, Skills/drop-close/, Skills/build-close/
---

# ⚠️ DEPRECATED

This prompt has been converted to skills:

- **Normal threads:** `Skills/thread-close/`
- **Pulse Drops:** `Skills/drop-close/`
- **Post-build:** `Skills/build-close/`

Use the appropriate skill instead.

Original archived at: `Prompts/Archive/Close Conversation.prompt.md`
EOF
```

#### Step 3: Delete old scripts (CAREFUL)

First verify no other code depends on them:

```bash
# Check for imports of old scripts
grep -r "from conversation_end" /home/workspace --include="*.py" 2>/dev/null
grep -r "import conversation_end" /home/workspace --include="*.py" 2>/dev/null

# Check for direct calls
grep -r "conversation_end_router\|conversation_end_quick\|conversation_end_standard\|conversation_end_full" /home/workspace --include="*.py" --include="*.md" 2>/dev/null | grep -v "N5/lib/close"
```

If no dependencies, delete:

```bash
# Only if safe!
rm /home/workspace/N5/scripts/conversation_end_router.py
rm /home/workspace/N5/scripts/conversation_end_quick.py
rm /home/workspace/N5/scripts/conversation_end_standard.py
rm /home/workspace/N5/scripts/conversation_end_full.py
```

**KEEP** `conversation_pii_audit.py` — it's used by `N5/lib/close/pii.py`

### Phase 4: Update References

If any rules, scheduled tasks, or other prompts reference "Close Conversation", update them to reference the new skills.

Common places to check:
- `list_rules()` — Check for close-related rules
- `list_agents()` — Check for scheduled tasks
- Other prompts in `/home/workspace/Prompts/`

### Phase 5: Final Verification

```bash
# Verify skill structure
ls -la Skills/thread-close/
ls -la Skills/drop-close/
ls -la Skills/build-close/
ls -la Skills/prompt-to-skill/
ls -la N5/lib/close/

# Verify redirect stub exists
cat "Prompts/Close Conversation.prompt.md"

# Verify archive exists
ls -la "Prompts/Archive/Close Conversation.prompt.md"
```

## Success Criteria

- [ ] All 6 integration tests pass
- [ ] Old prompt archived to `Prompts/Archive/`
- [ ] Redirect stub created at original location
- [ ] Old scripts deleted (except pii_audit)
- [ ] No dangling references
- [ ] All skills importable and runnable

## Deposit Format

```json
{
  "drop_id": "D3.1",
  "status": "complete",
  "summary": "Integration tests passed, original prompt archived, old scripts expunged",
  "artifacts": [
    "Prompts/Archive/Close Conversation.prompt.md",
    "Prompts/Close Conversation.prompt.md (redirect stub)"
  ],
  "learnings": "",
  "concerns": "",
  "decisions": [
    {
      "decision": "Kept conversation_pii_audit.py in N5/scripts",
      "rationale": "Used by N5/lib/close/pii.py as wrapper"
    }
  ]
}
```
