---
drop_id: D2.1
build_slug: prompt-to-skill
stream: 2
depends_on: ["D1.2"]
spawn_mode: auto
status: pending
thread_title: "[prompt-to-skill] D2.1: Create thread-close Skill"
---

# D2.1: Create thread-close Skill

## Mission

Create the `thread-close` skill for normal interactive threads. This is a thin wrapper around `N5/lib/close/` with fail-safe guards.

## Scope

### Files to Create

```
Skills/thread-close/
├── SKILL.md
└── scripts/
    └── close.py
```

### Must Not Touch
- Skills/drop-close/ (D2.2 scope)
- Skills/build-close/ (D2.3 scope)
- N5/lib/close/ (D1.2 created this)

## MUST DO

### 1. SKILL.md

```markdown
---
name: thread-close
description: |
  Close normal interactive conversation threads. Handles tier detection,
  position extraction, AAR generation, and commit suggestions.
  For Pulse workers use drop-close. For post-build synthesis use build-close.
---

# Thread Close

Close interactive conversation threads with semantic synthesis.

## When to Use

- Normal conversation threads
- Manual orchestrator threads
- Any thread that is NOT a Pulse Drop

**Wrong tool?**
- If you're a Pulse Drop → use `drop-close`
- If closing a completed build → use `build-close`

## Quick Start

```bash
# Auto-detect tier and close
python3 Skills/thread-close/scripts/close.py --convo-id con_XXXXX

# Force specific tier
python3 Skills/thread-close/scripts/close.py --convo-id con_XXXXX --tier 3

# Dry run (preview only)
python3 Skills/thread-close/scripts/close.py --convo-id con_XXXXX --dry-run
```

## Tiers

| Tier | When | What It Does |
|------|------|--------------|
| 1 | Simple discussions (<3 artifacts) | Title, summary, SESSION_STATE audit |
| 2 | Standard work (3-10 artifacts) | + Decisions, next steps |
| 3 | Builds, complex work (10+ artifacts) | + AAR, positions, content library |

## Options

- `--convo-id` (required): Conversation ID
- `--tier N`: Force tier 1, 2, or 3
- `--dry-run`: Preview without making changes
- `--force`: Bypass context guards (use carefully)
- `--skip-positions`: Skip position extraction

## What It Does

1. **Validates context** — Guards check you're not a Drop or build
2. **Detects tier** — Based on artifacts, conversation type
3. **Generates title** — 3-slot emoji system (State | Type | Content)
4. **Extracts decisions** — Key choices with rationale (Tier 2+)
5. **Generates AAR** — After-action report (Tier 3)
6. **Extracts positions** — Belief candidates (Tier 3)
7. **Scans content library** — Reusable artifacts (Tier 3)
8. **PII audit** — Checks for sensitive data

## Output

```
Running thread-close (Tier 2) for con_XXXXX

✓ Title: Jan 24 | ✅ 📌 🏗️ Build Close Skills
✓ Decisions extracted: 3
✓ PII audit complete

Thread close complete (Tier 2)
```

## Fail-Safes

This skill includes context guards. If you call it incorrectly:

```
⚠️  WRONG SKILL DETECTED

You called: thread-close
Suggested:  drop-close
Reason:     SESSION_STATE has drop_id

Run the suggested skill instead, or use --force to override.
```
```

### 2. scripts/close.py

```python
#!/usr/bin/env python3
"""Thread close CLI — wrapper around N5/lib/close with fail-safes."""

import argparse
import sys
from pathlib import Path

# Add N5 to path
sys.path.insert(0, '/home/workspace')

from N5.lib.close import guards, core

def main():
    parser = argparse.ArgumentParser(
        description='Close interactive conversation threads',
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog="""
Examples:
  %(prog)s --convo-id con_XXXXX
  %(prog)s --convo-id con_XXXXX --tier 3
  %(prog)s --convo-id con_XXXXX --dry-run
        """
    )
    parser.add_argument('--convo-id', required=True, help='Conversation ID')
    parser.add_argument('--tier', type=int, choices=[1, 2, 3], help='Force tier')
    parser.add_argument('--dry-run', action='store_true', help='Preview only')
    parser.add_argument('--force', action='store_true', help='Bypass guards')
    parser.add_argument('--skip-positions', action='store_true', help='Skip position extraction')
    
    args = parser.parse_args()
    
    # Load session state
    state = guards.load_session_state(args.convo_id)
    
    # FAIL-SAFE: Check context (unless --force)
    if not args.force:
        valid, reason = guards.validate_thread_context(state)
        if not valid:
            suggested = 'drop-close' if 'drop' in reason.lower() else 'build-close'
            guards.warn_wrong_skill(
                called='thread-close',
                suggested=suggested,
                reason=reason
            )
            return 1
    
    # Run thread close
    return core.run_thread_close(
        convo_id=args.convo_id,
        tier=args.tier,
        dry_run=args.dry_run
    )

if __name__ == '__main__':
    sys.exit(main())
```

## Verification

```bash
# Test help
python3 Skills/thread-close/scripts/close.py --help

# Test dry run on current conversation
python3 Skills/thread-close/scripts/close.py --convo-id con_plquQK5mpVEUO74p --dry-run

# Test guard (should warn if wrong context)
# Create mock drop context and verify guard triggers
```

## Deposit Format

```json
{
  "drop_id": "D2.1",
  "status": "complete",
  "summary": "Created thread-close skill with SKILL.md and close.py wrapper",
  "artifacts": [
    "Skills/thread-close/SKILL.md",
    "Skills/thread-close/scripts/close.py"
  ],
  "learnings": "",
  "concerns": "",
  "decisions": []
}
```
