---
created: 2026-01-19
worker_id: W1.1
build_slug: ode-ux-hardening
title: "[ode-ux-hardening] W1.1: Foundation - Quick Install + Version + Progress"
status: pending
---

# WORKER ASSIGNMENT: Foundation Layer

**Assigned to:** Zo (Builder Mode)
**Objective:** Create the foundational install infrastructure — VERSION file, quick-install.sh, progress tracking, and install.sh updates.

## Context from Parent

Building UX hardening for N5OS Ode. Users currently struggle with multi-step install. We're creating a one-liner install experience with clear progress tracking.

**Working directory:** `/home/workspace/N5/export/n5os-ode/`

## Scope

### Files You Own
- `VERSION` - CREATE
- `quick-install.sh` - CREATE  
- `SETUP_PROGRESS.md` - CREATE (template)
- `install.sh` - UPDATE
- `README.md` - UPDATE (add curl install)

### Must NOT Touch
- `BOOTLOADER.prompt.md` (W2.1 owns)
- `N5/personas/` (W1.2 owns)
- `N5/scripts/backup.py` (W2.1 owns)

## MUST DO

1. **Create VERSION file** at repo root:
   ```
   1.2.0
   ```

2. **Create quick-install.sh:**
   - Must work with: `curl -sL https://raw.githubusercontent.com/vrijenattawar/n5os-ode/main/quick-install.sh | bash`
   - Clone repo to temp location
   - Call install.sh
   - Clean output with emojis for each step

3. **Create SETUP_PROGRESS.md template:**
   - Table with 7 milestones (Files, Personas, Rules, Folders, DBs, Git, Personalized)
   - Each row: Step | Status (⬜/✅) | Completed timestamp
   - "Current Step" section with next action
   - Version section

4. **Update install.sh:**
   - After moving files, copy SETUP_PROGRESS.md to workspace root
   - Mark "1. Files Installed" as ✅ with timestamp
   - Update "Next Action" to "Run @BOOTLOADER.prompt.md"

5. **Update README.md:**
   - Add "One-Line Install" section at top of Quick Start
   - Show curl command prominently

## MUST NOT DO

- Do NOT modify BOOTLOADER.prompt.md
- Do NOT create persona files
- Do NOT create backup/rollback scripts
- Do NOT commit to git (orchestrator handles)

## Expected Output

1. Files created/updated as specified
2. Test: Run `bash quick-install.sh` (simulated) and verify it would work
3. Completion report in `completions/W1.1.json`

## Success Criteria

- [ ] VERSION file exists with "1.2.0"
- [ ] quick-install.sh is executable and has correct curl-compatible shebang
- [ ] SETUP_PROGRESS.md has all 7 milestones
- [ ] install.sh initializes progress file
- [ ] README.md has curl install instructions

---

**INSTRUCTION FOR SUB-AGENT:**

Work in `/home/workspace/N5/export/n5os-ode/`. Create all files as specified. Use `echo` or `cat > file << 'EOF'` for file creation. Test that quick-install.sh syntax is valid with `bash -n quick-install.sh`. When complete, write your completion report to `/home/workspace/N5/builds/ode-ux-hardening/completions/W1.1.json` with keys: worker_id, status, files_created, files_modified, issues, notes.
