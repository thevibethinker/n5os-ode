
## ⚠️ LESSON FROM PRIOR DROPS (READ FIRST)

D0.1 and D1.1 found that significant work already exists. Before implementing:

1. **CHECK EXISTING FIRST**: Search for files/functions that may already implement this
2. **VERIFY, DON'T DUPLICATE**: If implementation exists, verify it works rather than rewrite
3. **REPORT FINDINGS**: Note what already existed vs. what you created

This build is upgrading Pulse v1 → v2. Many components may already be partially implemented.


# D6.1: VibeTeacher Core

## Objective

Build the VibeTeacher integration that activates at HITL checkpoints to provide teaching moments and vocabulary building.

## Deliverables

1. **`N5/pulse/teaching/moment_generator.py`** — Generate teaching moments
2. **`N5/pulse/teaching/glossary.json`** — Technical vocabulary storage
3. **`N5/pulse/teaching/teaching_manager.py`** — Orchestrate VibeTeacher activations

## Implementation

### 1. Glossary Schema

Create `N5/pulse/teaching/glossary.json`:

```json
{
  "version": "1.0",
  "terms": [
    {
      "term": "MECE",
      "definition": "Mutually Exclusive, Collectively Exhaustive - a way to divide work so items don't overlap and nothing is missed",
      "v_description": "like those sort of non-overlapping worker assignments",
      "precise_term": "MECE decomposition",
      "context": "build planning",
      "first_encountered": "2026-01-24",
      "absorbed": false,
      "usage_count": 0
    }
  ]
}
```

### 2. Moment Generator

```python
#!/usr/bin/env python3
"""
Generate teaching moments from V's descriptions.
Detects imprecise language and surfaces precise alternatives.
"""

import json
from pathlib import Path

GLOSSARY_PATH = Path("N5/pulse/teaching/glossary.json")

def detect_imprecision(text: str) -> list:
    """
    Detect imprecise language patterns.
    
    Returns list of:
    {
        "v_phrase": "like those sort of non-overlapping things",
        "precise_term": "MECE decomposition",
        "explanation": "What you're describing is called MECE..."
    }
    """
    pass

def generate_moment(context: str, v_input: str, checkpoint: str) -> dict:
    """
    Generate a teaching moment.
    
    context: What's happening (plan_review, feedback, etc.)
    v_input: V's actual words
    checkpoint: Which HITL checkpoint triggered this
    
    Returns:
    {
        "has_moment": true/false,
        "teaching": {
            "you_said": "...",
            "precise_term": "...",
            "why_it_matters": "...",
            "example": "..."
        },
        "glossary_update": {...} or null
    }
    """
    pass

def add_to_glossary(term: str, definition: str, v_description: str, context: str):
    """Add new term to glossary."""
    pass

def mark_absorbed(term: str):
    """Mark term as absorbed (V demonstrated understanding)."""
    pass
```

### 3. Teaching Manager

```python
#!/usr/bin/env python3
"""
Orchestrate VibeTeacher activations at HITL checkpoints.
"""

ACTIVATION_POINTS = [
    "interview_complete",  # After seeded judgment
    "plan_review",         # When V reviews plan
    "feedback",            # When V provides feedback
    "build_complete"       # Final review
]

def activate_at_checkpoint(
    checkpoint: str,
    slug: str,
    v_input: str = None
) -> dict:
    """
    Activate VibeTeacher at a checkpoint.
    
    1. Analyze V's input for teaching opportunities
    2. Generate moment if found
    3. Store for later review (non-blocking)
    4. Return moment for display
    """
    pass

def get_pending_moments(slug: str) -> list:
    """Get teaching moments that haven't been acknowledged."""
    pass

def acknowledge_moment(moment_id: str, absorbed: bool = False):
    """
    Acknowledge a teaching moment.
    absorbed=True marks the term as learned.
    """
    pass
```

## Integration Points

### 1. After Interview Seeded
```python
# In seeded_judge.py, after marking seeded:
teaching_result = activate_at_checkpoint(
    "interview_complete",
    task_id,
    synthesis.get("combined_text")
)
```

### 2. During Plan Review
```python
# In review_manager.py, when V responds:
teaching_result = activate_at_checkpoint(
    "plan_review",
    slug,
    response
)
```

### 3. After Feedback
```python
# In feedback_extractor.py:
teaching_result = activate_at_checkpoint(
    "feedback",
    slug,
    feedback
)
```

## Moment Storage

Store moments at `N5/builds/<slug>/teaching_moments.jsonl`:

```jsonl
{"id": "tm-001", "checkpoint": "plan_review", "moment": {...}, "acknowledged": false, "absorbed": false}
```

## Success Criteria

- [ ] Glossary schema supports all required fields
- [ ] Moment generator detects imprecise language
- [ ] Teaching moments non-blocking (build continues)
- [ ] Moments stored with absorbed tracking
- [ ] Integration hooks at all 4 checkpoints

## Notes for Orchestrator

- D6.2 integrates this into the review flow
- Teaching is async — doesn't block build progress
- Glossary builds up over time, reducing repeat explanations
