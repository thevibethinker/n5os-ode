
## ⚠️ LESSON FROM PRIOR DROPS (READ FIRST)

D0.1 and D1.1 found that significant work already exists. Before implementing:

1. **CHECK EXISTING FIRST**: Search for files/functions that may already implement this
2. **VERIFY, DON'T DUPLICATE**: If implementation exists, verify it works rather than rewrite
3. **REPORT FINDINGS**: Note what already existed vs. what you created

This build is upgrading Pulse v1 → v2. Many components may already be partially implemented.


# D5.3: Persona Updates + Docs

## Objective

Update relevant personas with requirements tracking behavior, and update Pulse SKILL.md with v2 documentation.

## Deliverables

1. **Persona updates** — Add tracking behavior to Operator, Builder, Debugger, Architect
2. **`Skills/pulse/SKILL.md`** — Update with v2 features

## Persona Updates

Add to each persona (via edit_persona):

```markdown
## Requirements Tracking (During Builds)

When in a build context (build_slug present in session):
1. Monitor for requirement/preference/decision statements from V
2. Log via: `python3 N5/pulse/requirements_tracker.py capture "<text>" --type <req|pref|decision> --build-slug <slug>`
3. On errors/confusion, log telemetry: `python3 N5/pulse/telemetry_manager.py log <type> --data '{"message": "..."}' --build-slug <slug>`

This enables self-improvement feedback loops.
```

### Personas to Update

1. **Operator (90a7486f)** — Add tracking during build orchestration
2. **Builder (567cc602)** — Add tracking during implementation
3. **Debugger (17def82c)** — Add tracking during verification
4. **Architect (74e0a70d)** — Add tracking during planning

## SKILL.md Updates

Add to `Skills/pulse/SKILL.md`:

### v2 Features Section

```markdown
## Pulse v2 Features

### Task Queue
- Multi-channel intake: `n5 task <description>` via SMS, email "Task: X", or chat
- Task types: code_build, research, content, analysis, hybrid
- Commands: `queue_manager.py add|list|prioritize|advance|next`

### Interview System
- Fragment-based async collection
- Multi-channel aggregation (SMS + email + chat)
- JSONL storage: `N5/pulse/interviews/<task-id>/fragments.jsonl`
- Seeded judgment with LLM evaluation

### Plan Review
- Auto-sync to Google Drive: `Zo/Pulse Builds/<slug>/`
- SMS notification with shareable link
- Approval flow: "go" to build, "revise: X" for feedback

### Tidying Swarm
- 5 hygiene Drops post-build
- Auto-fix for safe issues
- Escalation for ambiguous findings

### Telemetry
- Persona + model attribution
- Requirements tracking
- Feedback → learnings pipeline
```

### Updated Lifecycle

```markdown
## v2 Lifecycle

pending → interviewing → seeded → planning → plan_review → building → tidying → complete

### Stage Transitions

| From | To | Trigger |
|------|----|---------|
| pending | interviewing | Task intake |
| interviewing | seeded | LLM judges ≥0.8 confidence |
| seeded | planning | `pulse.py plan <task>` |
| planning | plan_review | `pulse.py review <slug>` |
| plan_review | building | V responds "go" |
| building | tidying | All Drops complete |
| tidying | complete | Health score ≥0.9 or V approves |
```

## Success Criteria

- [ ] Operator persona includes tracking behavior
- [ ] Builder persona includes tracking behavior
- [ ] Debugger persona includes tracking behavior
- [ ] Architect persona includes tracking behavior
- [ ] SKILL.md documents all v2 features
- [ ] Lifecycle diagram updated

## Notes for Orchestrator

- This is the final Drop — completes Pulse v2
- Persona updates should be additive (don't remove existing behavior)
- SKILL.md is the canonical documentation
