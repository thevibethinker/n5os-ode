
## ⚠️ LESSON FROM PRIOR DROPS (READ FIRST)

D0.1 and D1.1 found that significant work already exists. Before implementing:

1. **CHECK EXISTING FIRST**: Search for files/functions that may already implement this
2. **VERIFY, DON'T DUPLICATE**: If implementation exists, verify it works rather than rewrite
3. **REPORT FINDINGS**: Note what already existed vs. what you created

This build is upgrading Pulse v1 → v2. Many components may already be partially implemented.


# D4.2: Tidying Orchestration

## Objective

Build the orchestration logic for the tidying swarm: spawn hygiene Drops, aggregate findings, dispatch fixes or escalate.

## Deliverables

1. **`N5/pulse/tidying_aggregator.py`** — Aggregate findings from hygiene Drops
2. **`N5/pulse/fix_dispatcher.py`** — Auto-fix vs escalate decision logic
3. **Update `Skills/pulse/scripts/pulse.py`** — Add tidying stage to lifecycle

## Implementation

### 1. Tidying Aggregator

```python
#!/usr/bin/env python3
"""Aggregate findings from tidying swarm Drops."""

def aggregate_findings(slug: str) -> dict:
    """
    Read all tidying deposits and aggregate.
    
    Returns:
    {
        "critical": [...],
        "warning": [...],
        "info": [...],
        "auto_fixable": [...],
        "health_score": 0.0-1.0
    }
    """
    pass

def compute_health_score(findings: dict) -> float:
    """
    Compute build health score.
    
    - 1.0 = no issues
    - <0.5 = critical issues present
    - 0.0 = build blocked
    """
    pass
```

### 2. Fix Dispatcher

```python
#!/usr/bin/env python3
"""Dispatch auto-fixes or escalate to V."""

def dispatch(slug: str, findings: dict) -> dict:
    """
    For each finding:
    - If auto_fixable AND confidence >= threshold: spawn fix Drop
    - Else: add to escalation report
    
    Config threshold from pulse_v2_config.json: tidying_swarm.auto_fix_threshold
    """
    pass

def generate_escalation_report(findings: list) -> str:
    """Generate SMS-friendly escalation summary."""
    pass
```

### 3. Pulse.py Tidying Stage

After all BUILD Drops complete:

```python
def enter_tidying_stage(slug: str):
    """
    1. Spawn 5 tidying Drops (parallel, one Stream)
    2. Wait for all deposits
    3. Run aggregator
    4. Run fix dispatcher
    5. If fixes spawned: wait, re-run checks
    6. If escalations: SMS summary, wait for approval
    7. Advance to complete/delivery
    """
    pass
```

## Tidying Flow

```
BUILD complete
      ↓
[Spawn 5 hygiene Drops in parallel]
      ↓
[Wait for all deposits]
      ↓
[Aggregate findings]
      ↓
health_score >= 0.9? ──YES──> DELIVERY
      │
      NO
      ↓
[Dispatch: auto-fix vs escalate]
      ↓
[If auto-fixes: spawn fix Drops → re-check]
[If escalations: SMS V → wait for response]
      ↓
DELIVERY
```

## Success Criteria

- [ ] Aggregator collects all 5 hygiene deposits
- [ ] Health score computed correctly
- [ ] Fix dispatcher respects threshold from config
- [ ] Auto-fix Drops spawn for eligible findings
- [ ] Escalation SMS generated for non-fixable issues
- [ ] pulse.py transitions through tidying stage correctly

## Notes for Orchestrator

- Depends on D4.1 (templates) for hygiene Drop briefs
- This is the last stage before delivery/feedback
- Tidying Drops use existing Pulse spawning mechanism
