---
created: 2026-02-02
drop_id: D1.3
build_slug: careerspan-pipeline-v2
stream: 1
spawn_mode: auto
depends_on: []
---
# D1.3 — Update Handler Skill

## Objective

Create `Skills/careerspan-update-handler/` — a skill that processes `[UPDATE]` tagged emails and updates Airtable accordingly.

## Context

When Shivam sends an `[UPDATE]` email, it could mean:
- Employer answered Core Questions
- Candidate status changed
- Role paused/closed
- General intel update

This skill needs to parse the update type and take appropriate action.

## Reference Files

- Config: `file 'Integrations/careerspan-pipeline/config.yaml'`
- Airtable schema: `file 'Integrations/careerspan-pipeline/SCHEMA_TO_ADD.md'`

## Update Types

| Type | Target | Example | Actions |
|------|--------|---------|---------|
| `employer_response` | Job Opening | "Guickly confirmed salary is $180-220k" | Update Core Question fields, possibly refresh Hiring POV |
| `candidate_status` | Candidate | "Jane Doe had first interview, going to round 2" | Update status field |
| `role_status` | Job Opening | "TechCorp role is paused" | Update intake_status, ball_in_court |
| `general_intel` | Any | "Employer prefers async workers" | Add to notes field |

## Skill Structure

```
Skills/careerspan-update-handler/
├── SKILL.md
├── scripts/
│   └── process_update.py
└── assets/
    └── (none needed initially)
```

## Script Interface

```bash
python3 Skills/careerspan-update-handler/scripts/process_update.py \
  --email-subject "..." \
  --email-body "..." \
  --email-from "..." \
  [--dry-run]
```

## Classification Approach

Use LLM (`/zo/ask`) to classify the update:
```
Given this email, classify the update type and extract the relevant information:

Email: {email_body}

Output JSON:
{
  "update_type": "employer_response|candidate_status|role_status|general_intel",
  "target_record_type": "job_opening|candidate|employer",
  "target_identifier": "company name or candidate name",
  "updates": {
    "field_name": "value"
  },
  "requires_response": boolean,
  "suggested_response": "..."
}
```

## Output

```json
{
  "update_type": "...",
  "records_updated": [
    {"table": "job_openings", "id": "rec...", "fields_updated": ["salary_min", "salary_max"]}
  ],
  "hiring_pov_refreshed": false,
  "email_sent": false
}
```

## Special Case: Core Questions Answered

If `update_type == "employer_response"` and Core Questions are now complete:
1. Update all checkbox fields
2. Optionally regenerate Hiring POV with new info
3. Update status to "Finalized" if all 5 answered
4. Email Shivam confirming completion

## Deposit Location

`/home/workspace/N5/builds/careerspan-pipeline-v2/deposits/D1.3-deposit.json`
