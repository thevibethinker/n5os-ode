---
drop_id: D2.2
build_slug: moltbook-modularization
stream: 4
depends_on: [D1.1, D1.2, D2.1]
spawn_mode: auto
spec_completeness: full
thread_title: "[moltbook-modularization] D2.2: End-to-end integration + validation"
---

# Drop Brief: End-to-end integration + validation

**Mission:** Integrate the modular pipeline end to end, verify the thin legacy orchestrator still works, and run the required build/validation gates for one-shot rollout readiness.

**Output:** Validated integrated code plus deposit describing verification results and any final cleanups.

---

## Context

This build is intended for a one-shot rollout. That means the modular pipeline must prove it can run through the same entrypoint, preserve staging/publish safety, and pass the build’s contract checks before handoff.

## MUST DO

- Integrate outputs from D1.1, D1.2, and D2.1 into a coherent `direct_poster.py` execution path
- Resolve interface mismatches and keep imports/package layout clean
- Run compile checks, dry-run checks, and required build validation commands
- Leave the branch/build in a handoff-ready state for Builder/execution continuation

## MUST NOT DO

- Do not reopen architectural scope already assigned to earlier drops unless required to resolve concrete integration defects
- Do not silently weaken pruning or modular boundaries for convenience
- Do not skip validation gates

## scope.files
- `Skills/zode-moltbook/scripts/direct_poster.py`
- `Skills/zode-moltbook/scripts/posting_pipeline/__init__.py`
- Any touched `posting_pipeline/*.py` files only as needed for final integration
- `N5/builds/moltbook-modularization/PLAN.md`
- `N5/builds/moltbook-modularization/meta.json`

## scope.responsibilities
- End-to-end integration
- Validation and test execution
- Final build-state readiness checks

## scope.must_not_touch
- Unrelated Moltbook scripts or external systems not needed for integration

## Scenarios

S1: Thin orchestrator still runs end to end
  Given: `direct_poster.py` remains the public entrypoint
  When: dry-run executes after modularization
  Then: the cycle completes through modular sensing, selection, grounding, critic, feedback, and publish-safe staging hooks without import/runtime failure
  Verify: `python3 Skills/zode-moltbook/scripts/direct_poster.py run --dry-run`

S2: Contract/validation gates pass
  Given: Pulse build discipline requires validation before execution claims
  When: build validation commands are run
  Then: contract and Pulse validation succeed or any warnings are explicitly documented
  Verify: `python3 N5/scripts/build_contract_check.py moltbook-modularization && python3 Skills/pulse/scripts/pulse.py validate moltbook-modularization`

S3: Compile safety holds
  Given: this is a multi-file Python refactor
  When: compile checks run
  Then: all touched scripts compile successfully
  Verify: `python3 -m py_compile Skills/zode-moltbook/scripts/direct_poster.py Skills/zode-moltbook/scripts/posting_pipeline/*.py`

## Expected Output

- Integrated modular pipeline
- Validation evidence in deposit
- Clear note of any residual warnings or follow-up tuning items
