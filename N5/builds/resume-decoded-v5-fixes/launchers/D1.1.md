---
drop: D1.1
spawn_mode: manual
created: 2026-02-15
---
# Drop D1.1 Launcher — Decomposer Signal Categorization

## Prompt for /zo/ask

You are working on Pulse build `resume-decoded-v5-fixes`, Drop D1.1.
Branch: `feature/resume-decoded-v5` (already checked out).

## Context

The Careerspan decomposer (`Skills/careerspan-decomposer/scripts/decompose.py`) produces a `scores_complete.json` file that includes signal strength percentages. Currently, for the Hardik-FlowFuse candidate, these are:

```json
"signal_strength": {
  "story_verified_pct": 76.7,
  "resume_only_pct": 20.9,
  "inferred_pct": 2.3
}
```

## Issues to Fix

### 1. Percentage rounding
The three values sum to 99.9%. Fix the calculation so they always sum to exactly 100% using integer rounding with remainder adjustment (largest remainder method).

### 2. Per-skill signal_type field
Each skill in the `skills` array should have an explicit `signal_type` field: `"story_verified"`, `"resume_only"`, or `"inferred"`. Verify this exists in the scoring prompt and in the output. If missing, add it to the LLM scoring prompt.

### 3. Inferred skill detection
The `inferred_pct` of 2.3% seems low. Review the scoring prompt to see if it instructs the LLM to identify skills that are contextually implied but not explicitly mentioned. For example: "Python not mentioned, but candidate worked at X fintech infra-heavy company — highly likely Python exposure." The decomposer should capture these as `inferred` signal type.

## Files to Read

- `Skills/careerspan-decomposer/scripts/decompose.py` — main script, look for signal_strength calculation and the scoring LLM prompt
- `Careerspan/meta-resumes/inbox/hardik-flowfuse/scores_complete.json` — current output
- `Careerspan/meta-resumes/inbox/hardik-flowfuse/hard_skills.yaml` — skill data

## Deliverable

1. Code changes to `decompose.py` (and `chunk_processor.py` if needed)
2. Write deposit to `N5/builds/resume-decoded-v5-fixes/deposits/D1.1.json` with:
   ```json
   {
     "drop": "D1.1",
     "status": "complete",
     "changes": ["list of files changed"],
     "summary": "what was fixed",
     "testing": "how it was verified"
   }
   ```

## Constraints
- Do NOT re-run the full decomposer pipeline (it requires LLM calls and takes time)
- Audit the code, make the fixes, verify logic correctness
- Stay on branch `feature/resume-decoded-v5`
