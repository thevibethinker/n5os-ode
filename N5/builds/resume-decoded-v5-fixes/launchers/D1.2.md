---
drop: D1.2
spawn_mode: manual
created: 2026-02-15
---
# Drop D1.2 Launcher — Adapter Signal Bar + Cross-Section Dedup

## Prompt for /zo/ask

You are working on Pulse build `resume-decoded-v5-fixes`, Drop D1.2.
Branch: `feature/resume-decoded-v5` (already checked out).

## Context

The Resume:Decoded adapter (`Skills/resume-decoded/scripts/adapter.py`) and template (`Skills/resume-decoded/templates/template.html`) have two bugs visible in the final PDF output.

## Bug 1: Signal percentages show 76% + 20% = 96% (missing 4%)

**Root cause:** `adapter.py` lines 946-947 only read `story_verified_pct` and `resume_only_pct`, ignoring `inferred_pct`. Template only renders 2 bars.

The decomposer produces 3 categories:
```json
"signal_strength": {
  "story_verified_pct": 76.7,
  "resume_only_pct": 20.9,
  "inferred_pct": 2.3
}
```

**Fix:**
1. In `adapter.py`, add `signal_inferred = int(signal.get("inferred_pct", 0))` at line ~948
2. Pass `signal_inferred` to the template data dict
3. In `template.html`, add a third bar for "Inferred" between Screen-verified and Resume-only, with a distinct mid-purple color
4. Ensure the three bar widths use percentages that sum to 100%

## Bug 2: "End-to-end solutions" appears as Strong Fit AND a question topic

**Root cause:** `generate_tradeoffs_llm()` (line 493) and `generate_interview_questions_llm()` (line 430) are independent LLM calls with no cross-referencing.

In the output:
- "Your Priorities → Fit" section: "Deliver end-to-end solutions" → Strong fit
- "Questions That Matter" section: "End-to-End Solution Delivery" → "Tell me about a time you owned a feature..."

This is contradictory: if it's a strong fit with screen evidence, why probe it?

**Fix:**
1. After both LLM calls return (around line 913 in `adapter.py`), add dedup logic:
   - Extract topic names from tradeoffs where verdict is "Strong fit"
   - Filter questions_all: remove any question whose topic fuzzy-matches a strong-fit tradeoff need
   - Use simple keyword overlap (lowercase, split on spaces, check >50% word overlap) — no need for full fuzzy matching library
2. Change the section subtitle in `template.html` from "Key areas to probe in the interview" to "Key areas to explore in the interview"
3. Optionally: rename evidence questions from "Confirms:" prefix to "Deepens:" to make clear these are about depth, not verification

## Files to Modify

- `Skills/resume-decoded/scripts/adapter.py` — signal reading (~line 946), dedup logic (~line 913)
- `Skills/resume-decoded/templates/template.html` — signal bar section, questions section header

## Test

After making changes:
1. Re-render from existing JSON data: `python3 Skills/resume-decoded/scripts/adapter.py --input-dir Careerspan/meta-resumes/inbox/hardik-flowfuse --output /tmp/hardik-v5-test.json`
2. Render HTML: use the template with the output JSON
3. Verify: signal bars sum to 100%, no topic overlap between strong-fit tradeoffs and questions

## Deliverable

Write deposit to `N5/builds/resume-decoded-v5-fixes/deposits/D1.2.json`:
```json
{
  "drop": "D1.2",
  "status": "complete",
  "changes": ["list of files changed"],
  "summary": "what was fixed",
  "testing": "how it was verified"
}
```

## Constraints
- Stay on branch `feature/resume-decoded-v5`
- Minimize LLM prompt changes — prefer post-processing dedup
- Keep function signatures stable
- Test by re-rendering the Hardik output
