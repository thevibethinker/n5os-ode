---
drop_id: D2.2
build_slug: pulse-self-upgrade
stream: 2
depends_on: ["D1.1", "D2.1", "D3.1", "D4.1"]
spawn_mode: auto
thread_title: "[pulse-self-upgrade] D2.2: SKILL.md Update"
---

# Drop Brief: SKILL.md Comprehensive Update

**Mission:** Update the main Pulse SKILL.md with documentation for all 4 new features.

**Output:** Updated `Skills/pulse/SKILL.md` with new sections for Forward Broadcast, Hypothesis Racing, Delegate-Only Mode, and Dynamic Task Claiming.

---

## Context

Wave 1 built 4 features. D1.2 tested them. This Drop updates the canonical documentation.

**New features to document:**
1. **Forward Broadcast** — `broadcast` field in deposits
2. **Hypothesis Racing** — `first_wins: true` mode
3. **Delegate-Only Mode** — Orchestrator coordination discipline
4. **Dynamic Task Claiming** — `task_pool` for worker Drops

## Files to Read

| File | Why Read It |
|------|-------------|
| `Skills/pulse/SKILL.md` | Current documentation — you'll add sections |
| `N5/builds/pulse-self-upgrade/deposits/D1.1.json` | What Forward Broadcast does |
| `N5/builds/pulse-self-upgrade/deposits/D2.1.json` | What Hypothesis Racing does |
| `N5/builds/pulse-self-upgrade/deposits/D3.1.json` | What Delegate-Only documents |
| `N5/builds/pulse-self-upgrade/deposits/D4.1.json` | What Task Claiming does |
| `Skills/pulse/references/delegate-only-mode.md` | Full delegate-only reference |

## Files to Modify

| File | Action | What to Do |
|------|--------|------------|
| `Skills/pulse/SKILL.md` | UPDATE | Add 4 new feature sections |

## Files NOT to Touch

- `pulse.py` — Feature implementation is done
- `pulse_integration_test.py` — D1.2 handled that
- `delegate-only-mode.md` — D3.1 created that

## Requirements

### 1. Add Forward Broadcast Section

Insert after "## Learnings System" or similar:

```markdown
## Forward Broadcast

Drops can share findings with subsequent Drops via the `broadcast` field in deposits.

### How It Works

1. Drop includes `broadcast` string in deposit JSON
2. Pulse collects all broadcasts from completed Drops
3. New Drops receive a "## Broadcasts from Prior Drops" section in their brief

### Deposit Schema

```json
{
  "drop_id": "D1.1",
  "status": "complete",
  "broadcast": "Auth tokens expire after 30 min—don't cache beyond that",
  ...
}
```

### Injected Format

```markdown
## Broadcasts from Prior Drops

These findings were shared by earlier Drops in this build:

- **D1.1:** Auth tokens expire after 30 min—don't cache beyond that
```

### Best Practices

- Keep broadcasts short (~500 chars max)
- Broadcast discoveries that affect other Drops
- Don't broadcast obvious things already in briefs
```

### 2. Add Hypothesis Racing Section

```markdown
## Hypothesis Racing

For debugging or exploration builds where you want to test multiple theories in parallel.

### Enabling

In `meta.json`:

```json
{
  "first_wins": true,
  "hypothesis_group": ["D1.1", "D1.2", "D1.3"]  // optional
}
```

### Verdict Field

Racing Drops include `verdict` in their deposit:

```json
{
  "drop_id": "D1.2",
  "status": "complete",
  "verdict": "confirmed",
  "summary": "Theory confirmed: rate limit is client-side"
}
```

Valid verdicts: `confirmed`, `rejected`, `inconclusive`

### Behavior

- When a Drop deposits `verdict: "confirmed"`, other Drops in the race are marked `superseded`
- Superseded Drops are not spawned (if pending) or ignored (if running)
- Wave can advance once winner confirms — superseded Drops don't block

### Use Cases

- Debugging with multiple theories
- A/B testing approaches
- Finding the first working solution among alternatives
```

### 3. Add Delegate-Only Mode Section

```markdown
## Delegate-Only Mode

Prevents the orchestrator from directly editing code, keeping all work in Drops.

### Enabling

In `meta.json`:

```json
{
  "delegate_only": true
}
```

### Constraints

When enabled, the orchestrator:
- **MAY**: Run pulse.py commands, read files, create/modify briefs, retry Drops
- **MUST NOT**: Edit source files, run application code, "fix" issues directly

### Why Use It

- Clear provenance (every change has a Drop source)
- Prevents long-build confusion
- Better audit trail

See `file 'Skills/pulse/references/delegate-only-mode.md'` for full details.
```

### 4. Add Task Pool Section

```markdown
## Task Pool (Dynamic Claiming)

For builds with many similar tasks, Drops can claim work from a shared pool.

### Enabling

In `meta.json`:

```json
{
  "task_pool": {
    "enabled": true,
    "tasks": [
      {"id": "T001", "type": "enrich", "target": "file1.json", "status": "pending"},
      {"id": "T002", "type": "enrich", "target": "file2.json", "status": "pending"}
    ],
    "max_concurrent_claims": 4,
    "worker_drops": ["D1.1", "D1.2", "D1.3", "D1.4"]
  }
}
```

### Task States

- `pending` — Available for claiming
- `claimed` — Assigned to a Drop
- `complete` — Finished successfully
- `failed` — Failed, may be re-claimable

### Claiming

Pool workers claim tasks atomically (file-locked to prevent races):

```python
task = claim_task(slug, drop_id)
if task is None:
    # Pool exhausted, exit
```

### Use Cases

- Processing batches of similar items
- Parallelizing uniform work without pre-planning assignments
- Variable-duration tasks where fast Drops should grab more work
```

### 5. Update Meta.json Structure

In the existing "## meta.json Structure" section, add the new fields:

```json
{
  "delegate_only": false,
  "first_wins": false,
  "hypothesis_group": [],
  "broadcasts": [],
  "task_pool": {
    "enabled": false,
    "tasks": [],
    "max_concurrent_claims": 4,
    "worker_drops": []
  }
}
```

### 6. Update Deposit Schema

Add `broadcast` and `verdict` to the deposit example:

```json
{
  "drop_id": "D1.1",
  "status": "complete",
  "broadcast": "Optional: finding to share with subsequent Drops",
  "verdict": "Optional: confirmed|rejected|inconclusive (for hypothesis racing)",
  ...
}
```

## Success Criteria

- [ ] Forward Broadcast section added with examples
- [ ] Hypothesis Racing section added with examples
- [ ] Delegate-Only Mode section added with link to reference
- [ ] Task Pool section added with examples
- [ ] meta.json structure updated with new fields
- [ ] Deposit schema updated with new optional fields
- [ ] No duplication with existing content
- [ ] Consistent formatting with rest of SKILL.md

## Anti-Patterns (Avoid)

- Don't duplicate the full delegate-only-mode.md content — link to it
- Don't remove existing content — only add
- Don't change the document structure dramatically — insert sections logically

---

## On Completion

Write deposit to `N5/builds/pulse-self-upgrade/deposits/D2.2.json`:

```json
{
  "drop_id": "D2.2",
  "status": "complete",
  "timestamp": "<ISO timestamp>",
  "summary": "<What sections were added>",
  "artifacts": [
    "Skills/pulse/SKILL.md"
  ],
  "decisions": [],
  "concerns": [],
  "notes_for_orchestrator": "SKILL.md fully updated with all new features"
}
```

**DO NOT COMMIT CODE.** The orchestrator commits at build completion.
