---
drop_id: D3.1
build_slug: pulse-self-upgrade
stream: 3
depends_on: []
spawn_mode: auto
thread_title: "[pulse-self-upgrade] D3.1: Delegate-Only Mode Docs"
---

# Drop Brief: Delegate-Only Mode Documentation

**Mission:** Document the `delegate_only` mode — a prompt discipline where the orchestrator refrains from directly editing code, instead only coordinating Drops.

**Output:** New section in `Skills/pulse/references/` explaining delegate-only mode.

---

## Context

This is part of a Pulse self-upgrade build. Delegate-only mode is NOT a code enforcement — it's a discipline documented in Pulse that the orchestrating conversation follows.

**Why this matters:**
- In long builds, the orchestrator can get confused and try to "help" by directly fixing issues
- This muddies provenance (who changed what?)
- It can cause merge conflicts with Drop work
- Delegate-only formalizes the separation of concerns

**Key insight:** This is primarily documentation, not code. The `delegate_only: true` flag in meta.json is a signal to the orchestrator (and to V reviewing the build) that direct intervention is prohibited.

## Files to Read

| File | Why Read It |
|------|-------------|
| `Skills/pulse/SKILL.md` | Current Pulse documentation — understand the style |
| `Skills/pulse/references/drop-brief-template.md` | Reference doc format |
| `N5/builds/pulse-self-upgrade/meta.json` | See how `delegate_only` is already set |

## Files to Create

| File | Action | What to Do |
|------|--------|------------|
| `Skills/pulse/references/delegate-only-mode.md` | CREATE | Full documentation of the mode |

## Files NOT to Touch

- `pulse.py` — No code enforcement (discipline, not code)
- `SKILL.md` — D2.2 will add a summary; this Drop creates the full reference doc
- Any other scripts

## Requirements

### 1. Create Reference Document

Create `Skills/pulse/references/delegate-only-mode.md` with:

```markdown
---
created: 2026-02-07
last_edited: 2026-02-07
version: 1.0
provenance: pulse-self-upgrade
---

# Delegate-Only Mode

## Overview

When `delegate_only: true` is set in a build's `meta.json`, the orchestrating conversation operates in **delegate-only mode** — it coordinates Drops but does NOT directly modify source files or run application code.

## Why Use It

- **Provenance clarity**: Every code change has a clear source (a specific Drop)
- **Prevents confusion**: Long builds can cause the orchestrator to "helpfully" intervene
- **Merge safety**: No risk of orchestrator edits conflicting with Drop work
- **Audit trail**: All work is captured in deposits, not conversation history

## When to Enable

Enable for:
- Large builds (5+ Drops)
- Builds where multiple Drops modify related files
- Builds where provenance matters for review
- Self-modifying builds (like Pulse upgrading itself)

## Orchestrator Constraints

When `delegate_only: true`, the orchestrator **MAY**:
- Run `pulse.py` commands (start, tick, stop, status, etc.)
- Read and review deposits
- Read source files (for context, not editing)
- Create/modify Drop briefs
- Retry failed Drops with better context
- Create new Drops for discovered work
- Update STATUS.md and meta.json
- Run `git status`, `git diff` (read-only git operations)

The orchestrator **MUST NOT**:
- Edit source files directly (*.py, *.ts, *.md in Sites/, Skills/, etc.)
- Run application code (except pulse.py orchestration commands)
- "Fix" issues found in deposits (create a retry Drop instead)
- Commit code (wait for finalization)
- Modify files in the build's `artifacts/` directory

## How to Enable

In `meta.json`:

```json
{
  "slug": "my-build",
  "delegate_only": true,
  ...
}
```

## What Happens If Violated

This is a **discipline**, not an enforcement. If the orchestrator violates delegate-only mode:

1. The build's audit trail is compromised
2. V may not notice which changes came from Drops vs. orchestrator
3. Git blame becomes unreliable

**Self-check**: Before any file edit, the orchestrator should ask: "Is this a pulse.py command, a brief, or meta.json? If not, don't touch it."

## Exceptions

The orchestrator MAY break delegate-only for:
- **Critical safety issues** (e.g., Drop is about to delete protected files)
- **V's explicit override** ("Go ahead and fix it directly")
- **Build-blocking infrastructure** (e.g., pulse.py itself is broken)

Document any exceptions in the build's STATUS.md with reason.

## Example Workflow

1. Orchestrator runs `pulse.py tick` → Drop D1.1 deposits with error
2. Orchestrator reads deposit, identifies issue
3. **Wrong**: Orchestrator edits the file to fix it
4. **Right**: Orchestrator runs `pulse.py retry D1.1 --reason "Missed X requirement"`
5. D1.1 re-runs with updated brief, fixes issue itself

## Related

- `file 'Skills/pulse/SKILL.md'` — Main Pulse documentation
- `file 'Skills/pulse/references/drop-brief-template.md'` — How to write good briefs
```

### 2. Content Quality

The document should be:
- Self-contained (readable without other docs)
- Practical (clear MAY/MUST NOT lists)
- Honest about enforcement (it's discipline, not code)

## Success Criteria

- [ ] `delegate-only-mode.md` exists in `Skills/pulse/references/`
- [ ] Has proper YAML frontmatter
- [ ] Explains when and why to use delegate-only
- [ ] Clear MAY/MUST NOT constraints
- [ ] Acknowledges it's discipline, not enforcement
- [ ] Includes example workflow

## Anti-Patterns (Avoid)

- Don't add code enforcement — this is documentation only
- Don't make it overly legalistic — keep it practical
- Don't duplicate content from SKILL.md — this is the detailed reference

---

## On Completion

Write deposit to `N5/builds/pulse-self-upgrade/deposits/D3.1.json`:

```json
{
  "drop_id": "D3.1",
  "status": "complete",
  "timestamp": "<ISO timestamp>",
  "summary": "<What was documented>",
  "broadcast": "Delegate-only mode documented — use 'delegate_only: true' in meta.json to signal orchestrator should coordinate only, not edit code",
  "artifacts": [
    "Skills/pulse/references/delegate-only-mode.md"
  ],
  "decisions": [],
  "concerns": [],
  "notes_for_orchestrator": "D2.2 should add a brief summary to SKILL.md pointing to this reference doc"
}
```

**DO NOT COMMIT CODE.** The orchestrator commits at build completion.
