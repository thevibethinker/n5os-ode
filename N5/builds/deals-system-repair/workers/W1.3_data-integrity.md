---
created: 2026-01-19
last_edited: 2026-01-19
version: 1.0
provenance: con_ZGBnCCZnbKMYnfcF
worker_id: W1.3
---

# Worker Brief: W1.3 - Data Integrity & Meeting Linkage

**Pre-decided thread title:** `[deals-system-repair] W1.3: Data Integrity & Linkage`

## Context
You are a worker in a build orchestration. This brief is self-contained. Do NOT ask clarifying questions—execute directly.

The deals database has data integrity issues: no `external_source` values, no meeting→deal linkages, and minimal deal_activities records. Your job is to fix these.

## Your Scope (MECE)
- Backfill `external_source` for existing deals
- Fix meeting→deal linkage (B36_DEAL_ROUTING files)
- Verify deal_activities population

**Out of scope:** Script fixes (W1.1), agent consolidation (W1.2)

## Current State
```
Database: /home/workspace/N5/data/deals.db
- 77 deals total (51 acquirers, 25 zo partnerships, 1 leadership)
- 0 deals have external_source populated
- 0 B36_DEAL_ROUTING files exist
- 2 deal_activities records (both Calendly)
- 328 meeting folders exist
```

## Tasks

### 1. Backfill external_source for existing deals

The deals have IDs that indicate their source:
- `zo-dp-*` → external_source should be "zo_partnerships_sheet"
- `cs-acq-*` → external_source should be "careerspan_acquirers_notion"
- `leadership-*` → external_source should be "careerspan_leadership_notion"

Write and run a SQL update:
```bash
sqlite3 /home/workspace/N5/data/deals.db "
UPDATE deals SET external_source = 'zo_partnerships_sheet' WHERE id LIKE 'zo-dp-%' AND (external_source IS NULL OR external_source = '');
UPDATE deals SET external_source = 'careerspan_acquirers_notion' WHERE id LIKE 'cs-acq-%' AND (external_source IS NULL OR external_source = '');
UPDATE deals SET external_source = 'careerspan_leadership_notion' WHERE id LIKE 'leadership-%' AND (external_source IS NULL OR external_source = '');
"
```

Verify:
```bash
sqlite3 /home/workspace/N5/data/deals.db "SELECT external_source, COUNT(*) FROM deals GROUP BY external_source;"
```

### 2. Test meeting→deal linkage with 1 meeting

The meeting router script exists and works:
```bash
# Generate prompt for 1 meeting
python3 /home/workspace/N5/scripts/deal_meeting_router.py --limit 1 --output-prompts /tmp/test_routing.jsonl

# Read the prompt
cat /tmp/test_routing.jsonl
```

The prompt needs to be processed by an LLM to generate routing output. For this test:
1. Read the prompt content
2. Analyze the meeting content yourself
3. Generate the B36_DEAL_ROUTING.json manually for that meeting
4. Write it to the meeting folder

B36_DEAL_ROUTING.json format:
```json
{
  "zo_relevance_score": 0.0-1.0,
  "careerspan_relevance_score": 0.0-1.0,
  "zo_reasoning": "explanation",
  "careerspan_reasoning": "explanation",
  "companies_mentioned": [],
  "recommended_deal_links": [],
  "create_new_deals": [],
  "summary": "1-2 sentence summary",
  "routed_at": "ISO timestamp"
}
```

Also create B36_DEAL_ROUTING.md (human-readable version).

### 3. Verify the meeting linkage
If a meeting has zo_relevance_score >= 0.70 or careerspan_relevance_score >= 0.70:
- Add entry to `deal_activities` table linking meeting to deal
- Update deal's `source_meetings` field

### 4. Document the routing workflow
After testing 1 meeting, document what would need to happen to route all 328 meetings:
- Is the workflow manual (LLM in the loop)?
- Could it be automated with `/zo/ask` batch?
- What's the recommended approach?

## Completion Report
When done, write your completion report to:
`/home/workspace/N5/builds/deals-system-repair/completions/W1.3.json`

Format:
```json
{
  "worker_id": "W1.3",
  "status": "complete|blocked|partial",
  "summary": "Brief description of what was done",
  "results": {
    "external_source_backfilled": {
      "zo_partnerships_sheet": <count>,
      "careerspan_acquirers_notion": <count>,
      "careerspan_leadership_notion": <count>
    },
    "meetings_routed": 1,
    "deal_activities_created": <count>,
    "routing_workflow_documented": true|false
  },
  "issues": ["any blockers or concerns"],
  "recommendations": ["for routing remaining 327 meetings"],
  "files_created": ["list of B36 files created"]
}
```

## Constraints
- Do NOT commit to git (orchestrator handles final commit)
- Do NOT modify scripts
- Do NOT touch scheduled agents
- Use idempotent operations (safe to re-run)
- If blocked, write completion report with status "blocked" and explain why
