---
created: 2026-01-19
last_edited: 2026-01-19
version: 1.0
provenance: con_ZGBnCCZnbKMYnfcF
worker_id: W2.1
---

# Worker Brief: W2.1 - Fix Leadership Record Types

**Pre-decided thread title:** `[deals-system-repair] W2.1: Fix Leadership Record Types`

## Context
Wave 1 sync created 21 leadership records with wrong `deal_type`. They have:
- IDs matching pattern `cs-lead-*`
- Company names like `[Leadership: PersonName]`
- `deal_type='careerspan_acquirer'` (WRONG - should be `leadership`)
- Missing `external_source` field

## Tasks

### 1. Verify the scope
```bash
sqlite3 /home/workspace/N5/data/deals.db "SELECT COUNT(*) FROM deals WHERE id LIKE 'cs-lead-%';"
# Expected: 21

sqlite3 /home/workspace/N5/data/deals.db "SELECT id, company, deal_type FROM deals WHERE id LIKE 'cs-lead-%' LIMIT 5;"
```

### 2. Fix deal_type for all cs-lead-* records
```bash
sqlite3 /home/workspace/N5/data/deals.db "UPDATE deals SET deal_type='leadership' WHERE id LIKE 'cs-lead-%';"
```

### 3. Set external_source for these records
```bash
sqlite3 /home/workspace/N5/data/deals.db "UPDATE deals SET external_source='careerspan_leadership_notion' WHERE id LIKE 'cs-lead-%';"
```

### 4. Verify the fix
```bash
# Check type distribution
sqlite3 /home/workspace/N5/data/deals.db "SELECT deal_type, COUNT(*) FROM deals GROUP BY deal_type;"
# Expected: careerspan_acquirer|51, leadership|22, zo_partnership|25

# Check external_source coverage
sqlite3 /home/workspace/N5/data/deals.db "SELECT COUNT(*) FROM deals WHERE external_source IS NULL OR external_source = '';"
# Expected: 0
```

### 5. Run deal_cli.py summary to confirm
```bash
python3 /home/workspace/N5/scripts/deal_cli.py summary
```

## Expected Outcome
- 22 records with `deal_type='leadership'` (1 existing + 21 fixed)
- 51 records with `deal_type='careerspan_acquirer'`
- 25 records with `deal_type='zo_partnership'`
- 98 total records, all with `external_source` populated

## Completion Report
Write to `/home/workspace/N5/builds/deals-system-repair/completions/W2.1.json`:
```json
{
  "worker_id": "W2.1",
  "status": "complete|blocked",
  "summary": "brief description",
  "results": {
    "records_fixed": 21,
    "deal_type_distribution": {"careerspan_acquirer": 51, "leadership": 22, "zo_partnership": 25},
    "external_source_coverage": "98/98"
  },
  "issues": [],
  "completed_at": "ISO timestamp"
}
```

## Constraints
- Do NOT commit to git (orchestrator handles final commit)
- These are simple SQL UPDATEs - no script changes needed
- If any record doesn't match expected pattern, note it in issues
