---
created: 2026-01-25
last_edited: 2026-01-25
version: 1.0
provenance: con_SjChSE4Zz2X5Hnui

drop_id: D2.1
build_slug: workos-integration
stream: 2
depends_on: ["D1.1", "D1.3"]
spawn_mode: auto
thread_title: "[workos-integration] D2.1: Validate auth locally (routes + gating)"
---

# Drop Brief: Validate auth locally (routes + gating)

**Mission:** Verify the auth integration works end-to-end in staging locally (login redirect, callback handler, cookie set, allowlist enforcement).

**Output (artifacts):**
- Deposit only: `N5/builds/workos-integration/deposits/D2.1.json`

---

## Context

Code is in `Sites/wheres-v-staging/server.ts` (Bun/Hono). Dependency `@workos-inc/node` is installed.

---

## Tasks

1) Run typecheck/build as appropriate for this repo (inspect `package.json` scripts).
2) Start server (or run existing dev command) and verify:
   - `/` redirects to `/login` when not authenticated
   - `/api/current-state` redirects to `/login` when not authenticated
   - `/login` returns a 302 to WorkOS AuthKit
   - `/callback` properly handles missing code (400 or redirect to /login)

If WorkOS redirect can’t be fully exercised without public URL, still verify route behavior and cookie logic.

---

## Success Criteria

- [ ] No TypeScript errors
- [ ] Server runs
- [ ] Core gating and redirect logic behave as expected

---

## On Completion

Write deposit to `N5/builds/workos-integration/deposits/D2.1.json` including commands run and observed behavior.
