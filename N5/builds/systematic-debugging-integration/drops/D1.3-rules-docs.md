---
build_slug: systematic-debugging-integration
drop_id: D1.3
wave: 1
stream: 3
order: 1
spawn_mode: auto
blocking: true
---

# D1.3: Update Rules & Docs

## Context

The conditional rule for "repeatedly encountering bugs" needs to reference the systematic-debugging skill. The debug-logging-auto-behavior.md needs to be updated to include skill phases in log entries.

## Scope

### Files (OWNS)
- Rule `cc80a90d-734b-4b9e-83ed-689d69cda16e` (repeated bugs conditional) - UPDATE via edit_rule
- `N5/prefs/operations/debug-logging-auto-behavior.md` - UPDATE

### Responsibilities
- Update rule to reference skill
- Update debug logging doc to include skill phase field
- Ensure backward compatibility (existing scripts still work)

### Must NOT Touch
- Debugger persona (owned by D1.2)
- The systematic-debugging skill files
- Other rules

## MUST DO

1. **Update repeated-bugs rule (cc80a90d):**

   Current condition: "When repeatedly encountering bugs or recurring coding issues"

   Update the instruction to add skill reference at the beginning:
   
   ```markdown
   **First, load the systematic debugging methodology:**
   Load and follow: file 'Skills/systematic-debugging/SKILL.md'

   Key checkpoints from the skill:
   - Phase 1: Root cause investigation BEFORE any fix attempt
   - Phase 3: Test ONE hypothesis at a time
   - Phase 4: If 3+ fixes fail, STOP and question the architecture

   Then ask these reflective questions:
   [keep existing questions list]
   ```

   Use `edit_rule` tool:
   ```python
   edit_rule(
       rule_id="cc80a90d-734b-4b9e-83ed-689d69cda16e",
       instruction="<updated instruction text>"
   )
   ```

2. **Update debug-logging-auto-behavior.md:**

   Add skill phase field to the log format. In the "Quick Command Templates" section, add `--skill-phase` parameter:

   ```bash
   python3 /home/workspace/N5/scripts/debug_logger.py append \
     --convo-id $CONVO_ID --component "$COMPONENT" \
     --problem "$PROBLEM" --hypothesis "$HYPOTHESIS" \
     --actions "$ACTIONS" --outcome success \
     --skill-phase "root_cause|pattern|hypothesis|implementation" \
     --notes "$NOTES"
   ```

   Add new section after "When to Log":
   
   ```markdown
   ## Skill Phase Tracking

   When logging debug attempts, include the skill phase from `systematic-debugging`:

   | Phase | Value | When to Use |
   |-------|-------|-------------|
   | Root Cause | `root_cause` | Investigating before any fix |
   | Pattern Analysis | `pattern` | Comparing working vs broken |
   | Hypothesis Testing | `hypothesis` | Testing a specific theory |
   | Implementation | `implementation` | Applying a fix |

   Example:
   ```bash
   python3 /home/workspace/N5/scripts/debug_logger.py append \
     --convo-id con_xyz --component "api-auth" \
     --problem "Token validation failing" \
     --hypothesis "Token format changed in v2" \
     --actions "Compared v1 and v2 token structures" \
     --outcome partial --skill-phase root_cause \
     --notes "Found format difference, need to trace source"
   ```
   ```

3. **Update file frontmatter:**

   Update last_edited and version in debug-logging-auto-behavior.md:
   ```yaml
   ---
   ...
   last_edited: '2026-02-07'
   version: 1.2
   ...
   ---
   ```

## MUST NOT DO

- Do NOT modify the debug_logger.py script (the --skill-phase flag is optional documentation for manual use)
- Do NOT modify other rules
- Do NOT modify the Debugger persona (that's D1.2's scope)
- Do NOT remove any existing content from the rule or doc

## Verification

1. **Rule verification:**
   ```python
   list_rules()  # Check cc80a90d contains "systematic-debugging"
   ```

2. **Doc verification:**
   ```bash
   grep -l "skill-phase" N5/prefs/operations/debug-logging-auto-behavior.md
   grep -l "systematic-debugging" N5/prefs/operations/debug-logging-auto-behavior.md
   ```

## Deposit Path

Write completion report to: `N5/builds/systematic-debugging-integration/deposits/D1.3.json`

## Deposit Schema

```json
{
  "drop_id": "D1.3",
  "status": "complete",
  "rule_updated": "cc80a90d-734b-4b9e-83ed-689d69cda16e",
  "doc_updated": "N5/prefs/operations/debug-logging-auto-behavior.md",
  "verification": {
    "rule_contains_skill": true,
    "doc_contains_skill_phase": true
  },
  "notes": "any observations"
}
```
