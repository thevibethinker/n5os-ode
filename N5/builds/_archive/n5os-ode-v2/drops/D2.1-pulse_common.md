---
drop_id: D2.1
build_slug: n5os-ode-v2
stream: S2
type: code_build
status: pending
spawn_mode: auto
---

# D2.1: Update Pulse Common with Workspace Env Var

## Context
Pulse scripts have hardcoded `/home/workspace` paths. Update the central pulse_common.py to use environment variables.

## Scope
```yaml
files:
  - Skills/pulse/scripts/pulse_common.py
must_not_touch:
  - Other Pulse scripts
```

## MUST DO
1. Add at module top:
   ```python
   import os
   WORKSPACE = Path(os.environ.get("ZO_WORKSPACE", "/home/workspace"))
   ```
2. Update PATHS class to use WORKSPACE as base for all paths
3. Ensure backward compatibility (defaults still work)
4. Test: `python3 -c "from Skills.pulse.scripts.pulse_common import PATHS; print(PATHS.workspace)"`

## MUST NOT DO
- Change any logic
- Touch other Pulse scripts

## EXPECTED OUTPUT
Deposit should confirm:
- File updated
- Import test passes
- Paste the updated PATHS class
