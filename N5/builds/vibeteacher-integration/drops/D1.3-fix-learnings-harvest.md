---
build_slug: vibeteacher-integration
drop_id: D1.3
thread_title: "[vibeteacher-integration] D1.3: Fix learnings harvest"
spawn_mode: auto
---

# D1.3: Fix Learnings Harvest Parsing

## Objective
Debug and fix why `learnings_harvested: 0` in FINALIZATION.json even when deposits contain learnings.

## Context
In the `pulse-v2` build finalization:
```json
{
  "learnings_harvested": 0,
  ...
}
```

But deposits DO contain learnings. The harvest step isn't extracting them properly.

## Files to Investigate
1. `/home/workspace/Skills/pulse/scripts/pulse_learnings.py` — the `harvest` command
2. `/home/workspace/N5/builds/pulse-v2/deposits/*.json` — sample deposits to check format

## Investigation Steps

1. Check how `pulse_learnings.py harvest` parses deposits:
```bash
python3 Skills/pulse/scripts/pulse_learnings.py harvest pulse-v2 --verbose
```

2. Check deposit format — does it have `learnings` field? Is it a string or list?

3. Common issues:
   - Field name mismatch (`learnings` vs `learning` vs `lessons`)
   - Type mismatch (expecting list, getting string)
   - Path issues finding deposits

## Implementation
Fix the parsing in `pulse_learnings.py` to correctly extract learnings from deposits.

## Success Criteria
- [ ] `pulse_learnings.py harvest <slug>` correctly counts learnings
- [ ] Learnings are written to BUILD_LESSONS.json
- [ ] Output shows "Harvested N learnings" with correct count
- [ ] Test with existing `pulse-v2` build

## Deposit Location
Write deposit to: `N5/builds/vibeteacher-integration/deposits/D1.3.json`

## Deposit Schema
```json
{
  "drop_id": "D1.3",
  "status": "complete|blocked",
  "summary": "Brief description of what was done",
  "artifacts": ["list of files modified"],
  "learnings": "Any insights for future work",
  "concerns": "Any issues or risks",
  "decisions": [{"decision": "what", "rationale": "why"}]
}
```
