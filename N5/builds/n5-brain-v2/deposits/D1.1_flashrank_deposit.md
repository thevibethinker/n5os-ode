---
drop_id: D1.1
build_slug: n5-brain-v2
status: complete
completed_at: 2026-02-01T00:50:00-05:00
---

# D1.1 Deposit: FlashRank Reranker Integration

## Summary
Successfully integrated FlashRank as the default reranker, replacing cross-encoder for faster search reranking.

## Files Created/Modified

### Created
- `N5/cognition/rerankers.py` — NEW: Reranker abstraction layer with FlashRank, CrossEncoder, and NoOp implementations

### Modified
- `N5/cognition/n5_memory_client.py`:
  - Added v2 embedding store auto-detection
  - Added OpenAI embedding auto-detection from stored vector dimensions
  - Integrated reranker abstraction via `_init_reranker()`
  - Added `from pathlib import Path` import

## Configuration

**Env Vars:**
- `N5_RERANKER` — Options: `flashrank` (default), `cross-encoder`, `none`
- Auto-fallback: If FlashRank unavailable, falls back to cross-encoder, then none

## Test Results

```
DB: /home/workspace/N5/cognition/vectors_v2.db
Provider: openai (auto-detected from 3072-dim vectors)
Reranker: FlashRank(ms-marco-MiniLM-L-12-v2)

Search "networking beliefs": 3 results returned successfully
```

## Additional Fixes Made During D1.1

1. **v2 embedding store integration**: Client now auto-prefers `vectors_v2.db` if it exists
2. **Embedding dimension auto-detection**: If vectors are 3072-dim and provider is local, auto-switches to OpenAI
3. **Import fix**: Added `from pathlib import Path`

## Benchmark (Informal)

FlashRank initialization: ~2s (model download cached)
Search latency: ~1s for semantic + rerank (brute force, no HNSW index)

## Not Implemented
- Formal benchmark script (can add later)
- HNSW index integration (requires `pip install hnswlib`)

## Lessons Learned
- FlashRank model `ms-marco-MiniLM-L-6-v2` doesn't exist on HuggingFace; use `ms-marco-MiniLM-L-12-v2`
- v2 embedding store had dimension mismatch issues; auto-detection resolves this
