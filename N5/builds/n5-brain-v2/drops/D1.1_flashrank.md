---
drop_id: D1.1
build_slug: n5-brain-v2
stream: 1
title: FlashRank Reranker Integration
spawn_mode: auto
status: pending
created: 2026-02-01
---

# Drop D1.1: FlashRank Reranker Integration

## Objective
Replace/augment the current cross-encoder reranker with FlashRank for faster, lighter reranking.

## Scope

### Files to Create/Modify
- `N5/cognition/n5_memory_client.py` — Add FlashRank reranker alongside existing cross-encoder
- `N5/cognition/rerankers.py` — NEW: Abstract reranker interface with FlashRank + CrossEncoder implementations

### Must NOT Touch
- Database schema (that's D1.2's job)
- Entity extraction (that's D2.1's job)
- Any files outside N5/cognition/

## Requirements

### MUST DO
1. Install FlashRank: `pip install flashrank`
2. Create `rerankers.py` with abstract `Reranker` base class
3. Implement `FlashRankReranker` class
4. Implement `CrossEncoderReranker` class (extract from current code)
5. Add config: `N5_RERANKER` env var (values: `flashrank`, `cross-encoder`, default: `flashrank`)
6. Update `n5_memory_client.py` to use configured reranker
7. Run benchmark: same 10 queries, compare latency and result quality
8. Document benchmark results in deposit

### MUST NOT DO
- Change database schema
- Modify search logic beyond swapping reranker
- Remove cross-encoder (keep as fallback option)

## Success Criteria
- [ ] FlashRank reranker returns sorted results
- [ ] Config flag switches between rerankers correctly
- [ ] Benchmark shows latency improvement
- [ ] Existing tests still pass
- [ ] No regressions in search quality

## Expected Output
1. Modified `n5_memory_client.py`
2. New `rerankers.py`
3. Benchmark results (latency comparison table)

## Context Files
- `file 'N5/cognition/n5_memory_client.py'` — Current implementation
- `file 'N5/builds/n5-brain-v2/PLAN.md'` — Full build plan

## Deposit Location
`/home/workspace/N5/builds/n5-brain-v2/deposits/D1.1_flashrank_deposit.md`
