---
drop_id: D2.1
build_slug: meeting-system-migration
name: Add Archive Command to Skill
stream: 2
depends_on: ["D1.1", "D1.2"]
spawn_mode: auto
---

# D2.1: Add Archive Command to Meeting-Ingestion Skill

## Context

The Weekly Meeting Organization agent (9b813d5c) currently moves processed meetings to weekly archive folders. This functionality should be folded into the meeting-ingestion skill as an `archive` command.

Current agent logic:
- Scans `Personal/Meetings/Inbox/` for meetings with status="complete" in manifest.json
- Moves them to `Personal/Meetings/Week-of-YYYY-MM-DD/` folders
- Runs 2x daily (3:30am and 3:30pm)

## Scope

### Files to Create/Modify
- `Skills/meeting-ingestion/scripts/archive.py` - CREATE - Archive logic
- `Skills/meeting-ingestion/scripts/meeting_cli.py` - UPDATE - Add archive subcommand
- `Skills/meeting-ingestion/SKILL.md` - UPDATE - Document archive command

### Must NOT Touch
- `N5/scripts/meeting_*.py` - Core meeting scripts
- Any scheduled agents (that's D3.1)
- Any prompts

## MUST DO

1. **Read the current Weekly Meeting Organization agent** to understand exact logic:
   - Agent ID: 9b813d5c-187b-4293-bb4f-652319043704
   - Uses manifest.json status field (not folder suffix) to determine completion

2. **Create `archive.py`** with:
   ```python
   # Core functions:
   # - scan_completed_meetings() - Find meetings with status=complete
   # - get_target_week_folder(meeting_date) - Calculate Week-of-YYYY-MM-DD
   # - archive_meeting(meeting_path, target_folder) - Move meeting
   # - main() - CLI entry point
   ```

3. **Update `meeting_cli.py`** to add:
   ```
   meeting_cli.py archive [--dry-run]
   ```

4. **Update `SKILL.md`** with archive documentation

5. **Test the archive command**:
   ```bash
   python3 Skills/meeting-ingestion/scripts/meeting_cli.py archive --dry-run
   ```

## MUST NOT DO

- Do not modify or delete any scheduled agents
- Do not change N5/scripts/ files
- Do not move actual meetings (only test with --dry-run)

## Expected Output

Deposit JSON with:
```json
{
  "status": "complete",
  "summary": "Added archive command to meeting-ingestion skill",
  "files_created": ["Skills/meeting-ingestion/scripts/archive.py"],
  "files_modified": [
    "Skills/meeting-ingestion/scripts/meeting_cli.py",
    "Skills/meeting-ingestion/SKILL.md"
  ],
  "test_results": {
    "archive_dry_run": "pass|fail",
    "cli_help": "pass|fail"
  },
  "artifacts": []
}
```
