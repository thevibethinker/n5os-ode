---
drop_id: D1.1
name: Webhook Bug Fixes + Voice Tuning
wave: W1
stream: 1
order: 1
spawn_mode: auto
blocking: true
depends_on: []
---

# D1.1: Webhook Bug Fixes + Voice Tuning

## Context

You are fixing 5 production bugs/config issues in the Zo Hotline webhook. The hotline is a VAPI voice AI service running as a Bun webhook on port 4243. The webhook source is at `Skills/zo-hotline/scripts/hotline-webhook.ts`.

**Critical context:** The v5 deploy broke the analysisPlan — the last 2 substantive calls have zero analysis data (no summary, no structuredData, no successEvaluation). This is the highest priority fix.

**Amanda's call (019c730d):** 257 seconds, 6 interruptions, ended with silence-timed-out. The call data is in DuckDB at `Datasets/zo-hotline-calls/data.duckdb`.

## Scope

### Files You Own
- `Skills/zo-hotline/scripts/hotline-webhook.ts` — Bug fixes and config changes ONLY (do NOT add new tools or features)

### Must Not Touch
- `Skills/zo-hotline/prompts/zoseph-system-prompt.md` — Owned by D1.2
- Any zo.space routes — Owned by D2.1
- `Skills/zo-hotline/IMPROVEMENT_PLAN.md` — Owned by D2.2

## MUST DO

### 1. Fix analysisPlan (HIGHEST PRIORITY)

The nested `summaryPlan`/`structuredDataPlan`/`successEvaluationPlan` format introduced in v5 is not producing data. 

**Steps:**
1. Check VAPI docs for the correct analysisPlan schema. Use `agent-browser` or `read_webpage` to check https://docs.vapi.ai for the current assistant schema.
2. Check the status-update logs in `/dev/shm/zo-hotline-webhook.log` for validation errors on recent calls.
3. If the nested format is wrong, revert to the flat format that worked pre-v5:
   ```typescript
   analysisPlan: {
     summary: { enabled: true, messages: [...] },
     structuredData: { enabled: true, schema: {...}, messages: [...] },
     successEvaluation: { enabled: true, rubric: "NumericScale", messages: [...] }
   }
   ```
4. If docs confirm the nested format is correct, fix whatever field name mismatch exists.

**Verification:** After restart, the next substantive call should have non-null `analysis.summary` in its raw_data.

### 2. Fix tool_usage logging

`trackToolUsage()` and `persistToolUsage()` functions exist but no data is being written. Debug:
1. Check if `tool_usage.jsonl` path is correct (should be in `Datasets/zo-hotline-calls/` or `Skills/zo-hotline/scripts/`)
2. Check if `initDb()` creates a `tool_usage` table — if not, add DDL
3. Ensure `persistToolUsage` is actually called and writes successfully
4. Add error logging if writes fail

### 3. Tune interruption parameters

Change these values in the assistant-request response:

```typescript
stopSpeakingPlan: {
  numWords: 2,        // was 1 (any single word interrupted)
  voiceSeconds: 0.5,  // was 0.3 (too hair-trigger)
  backoffSeconds: 1.5  // was 1.0
}

startSpeakingPlan: {
  waitSeconds: 0.8,   // was 0.6
  smartEndpointingEnabled: true,
  transcriptionEndpointingPlan: {
    onPunctuationSeconds: 0.5,   // was 0.3 (jumped in mid-sentence)
    onNoPunctuationSeconds: 1.5, // was 1.0
    onNumberSeconds: 0.6         // was 0.4
  }
}
```

### 4. Harden Zo/Zoho/Zoom transcription

Amanda's call had "ZOOM computer" for "Zo Computer." In the Deepgram transcriber config:
1. Check if `keywords` or `keyterm` config exists. If yes, strengthen it.
2. Add keyword boosting: `Zo Computer` (boost high), `Zo` (boost high), `Zoseph` (boost medium)
3. If using nova-2, check if nova-3 is available on VAPI (nova-3 supports `keyterm` better). Only switch if VAPI supports it — if unsure, stay on nova-2 with keyword boosting.

### 5. Add silence timeout columns to DuckDB

For the follow-up system (D2.2), we need columns on the `calls` table:
```sql
ALTER TABLE calls ADD COLUMN IF NOT EXISTS followup_sent BOOLEAN DEFAULT FALSE;
ALTER TABLE calls ADD COLUMN IF NOT EXISTS followup_url VARCHAR;
ALTER TABLE calls ADD COLUMN IF NOT EXISTS followup_sent_at TIMESTAMP;
```
Add these to `initDb()` function. This is safe to add now — D2.2 will populate them.

## MUST NOT DO

- Do NOT add new tools (sendFollowUp is D2.2's job)
- Do NOT modify the system prompt
- Do NOT change the first message text
- Do NOT add new features — this is a bug fix and tuning drop only
- Do NOT change the VAPI model (stay on claude-haiku-4-5-20251001)

## EXPECTED OUTPUT

Deposit JSON with:
```json
{
  "drop_id": "D1.1",
  "status": "complete",
  "summary": "Fixed 5 issues: analysisPlan schema, tool logging, interruption tuning, transcription hardening, DDL for followup columns",
  "files_modified": ["Skills/zo-hotline/scripts/hotline-webhook.ts"],
  "broadcast": "<key finding about analysisPlan format that D2.2 needs to know>",
  "verification": {
    "analysisPlan_fix": "reverted to flat format / fixed nested format",
    "tool_logging": "fixed — writes to <path>",
    "interruption_params": "numWords=2, voiceSeconds=0.5, waitSeconds=0.8",
    "transcription": "keyword boosting added for Zo/Zoho/Zoom",
    "ddl": "followup columns added to initDb"
  }
}
```

Write deposit to: `N5/builds/zo-hotline-v6/deposits/D1.1.json`

After completing changes, restart the hotline service:
```bash
# Find the service and get its entrypoint
cat /dev/shm/zo-hotline-webhook.log | head -5
# The service runs on port 4243
```
Verify the webhook starts cleanly by checking logs.
