---
drop_id: D1.2
build_slug: semantic-memory-google-migration
stream: 2
depends_on: []
spawn_mode: auto
spec_completeness: full
thread_title: "[semantic-memory-google-migration] D1.2: Gemini provider adapter + indexing alignment"
---

# Drop Brief: Gemini provider adapter + indexing alignment

**Mission:** Implement Gemini embedding provider support using `GEMINI_API_KEY`, make Google the only active embedding provider, and align indexing/ANN scripts with the new provider and canonical `brain.db` path.

**Output:** Updated provider and indexing scripts plus a deposit describing model/config/dimension decisions.

---

## Context

V explicitly chose Gemini API, Google-only provider strategy, and full migration. The current system is OpenAI-biased in both provider initialization and indexing scripts. This drop owns the provider adapter and indexing script alignment, but not the higher-level migration runner or eval harness.

## MUST DO

- Add Gemini embedding provider support using `GEMINI_API_KEY`
- Make provider selection deterministic and compatible with a Google-only system
- Align indexing and ANN rebuild scripts with the canonical `brain.db` path
- Make embedding dimension/model configuration explicit rather than implied by legacy OpenAI vectors
- Preserve script usability and existing operational ergonomics where possible

## MUST NOT DO

- Do not decide canonical DB ownership; D1.1 owns that substrate contract
- Do not build the migration-runner/resumability flow; D2.1 owns that
- Do not build evaluation/cutover scoring; D2.2 owns that
- Do not assume old OpenAI ANN indexes can be reused directly

## scope.files
- `N5/cognition/n5_memory_client.py`
- `N5/scripts/n5_index_embeddings.py`
- `N5/scripts/n5_rebuild_ann_index.py`
- `N5/scripts/migrate_embeddings.py`
- `N5/scripts/run_full_reindex.py`

## scope.responsibilities
- Gemini provider adapter
- Provider/model/dimension config
- Indexing script alignment
- ANN rebuild script compatibility

## scope.must_not_touch
- `N5/cognition/BASELINE.md`
- `N5/scripts/semantic_memory_reindex.py`
- `N5/scripts/semantic_memory_eval.py`

## Files to Read

| File | Why Read It |
|------|-------------|
| `N5/cognition/n5_memory_client.py` | Contains current OpenAI/local provider initialization and embedding call path |
| `N5/scripts/n5_index_embeddings.py` | Shows current OpenAI-centric indexing assumptions and dimension checks |
| `N5/scripts/n5_rebuild_ann_index.py` | Rebuild script must continue to work against canonical vectors |
| `N5/scripts/migrate_embeddings.py` | May contain reusable migration/provider logic |
| `N5/scripts/run_full_reindex.py` | Helps align full reindex workflows with new provider assumptions |

## Files to Modify/Create

| File | Action | What to Do |
|------|--------|------------|
| `N5/cognition/n5_memory_client.py` | UPDATE | Add Gemini provider initialization and embedding call path |
| `N5/scripts/n5_index_embeddings.py` | UPDATE | Use canonical DB + Gemini-aware provider/dimension logic |
| `N5/scripts/n5_rebuild_ann_index.py` | UPDATE | Keep rebuild aligned with canonical DB/provider setup |
| `N5/scripts/migrate_embeddings.py` | UPDATE | Reuse or adapt for Google-only embedding migration if useful |
| `N5/scripts/run_full_reindex.py` | UPDATE | Ensure full reindex flow honors new provider configuration |

## Files NOT to Touch

- `N5/lib/paths.py` — D1.1 owns canonical path semantics
- `N5/cognition/BASELINE.md` — D1.1/D2.2 own architecture/eval narrative
- `N5/scripts/semantic_memory_reindex.py` — D2.1 owns migration runner
- `N5/scripts/semantic_memory_eval.py` — D2.2 owns eval harness

## Scenarios

S1: Gemini embeddings can be produced through config
  Given: `GEMINI_API_KEY` is configured in Zo secrets
  When: the memory client is initialized for Google/Gemini
  Then: it can request embeddings successfully without OpenAI dependency
  Verify: run a minimal Python check that instantiates the client and generates one embedding

S2: Indexing scripts no longer assume OpenAI-only 3072 behavior
  Given: the old scripts were tuned for OpenAI large embeddings
  When: indexing scripts run under the new provider path
  Then: model/provider/dimension assumptions are explicit and compatible with Gemini
  Verify: inspect script flags/config and run dry-run/help or a small indexing test

S3: ANN rebuild stays compatible with canonical vectors
  Given: provider/model may change vector shape and rebuild requirements
  When: ANN rebuild runs after Gemini-based indexing
  Then: it rebuilds from canonical vectors instead of assuming old OpenAI indexes
  Verify: run ANN rebuild on a small test corpus or inspect the rebuild path for canonical-source behavior

## Success Criteria

- [ ] Gemini provider path exists and uses `GEMINI_API_KEY`
- [ ] OpenAI is no longer required for active embedding generation
- [ ] Indexing scripts are Gemini-aware and target canonical DB behavior
- [ ] ANN rebuild script remains operational under the new setup

## Anti-Patterns (Avoid)

- Don’t preserve hidden OpenAI fallbacks in the active path
- Don’t hardcode ambiguous dimensions without config/documented rationale
- Don’t try to reuse stale OpenAI-built indexes as-is

## Expected Output

- Provider/indexing code changes for Google-only embeddings
- Deposit summarizing chosen model defaults, dimension behavior, and any unresolved compatibility questions
