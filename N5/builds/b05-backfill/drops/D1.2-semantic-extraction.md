---
drop_id: D1.2
build_slug: b05-backfill
title: Semantic Extraction Pipeline
stream: 1
depends_on: []
spawn_mode: auto
---

# D1.2: Semantic Extraction Pipeline

## Context

V wants real task backfill from meeting action items, not fake "Prepare for X" garbage. 

**Critical mandate:** Use LLM semantic reasoning, NOT regex/Python pattern matching.

34 B05 blocks were modified in the last 7 days. We need to:
1. Identify meetings with transcripts from last week
2. Regenerate B05 blocks through new decomposition prompt
3. Extract V's action items with urgency signals
4. Deduplicate semantically across meetings
5. Stage all items for V's review

## Scope

### Files
- `/home/workspace/Personal/Meetings/Week-of-2026-01-19/*/` — Read transcripts, write B05s
- `/home/workspace/Personal/Meetings/Week-of-2026-01-12/*/` — If in 7-day window
- `/home/.z/workspaces/con_BTFIhYsxr3P3CzaR/extraction_results.json` — Write intermediate results

### Responsibilities
- Find all transcripts from last 7 days
- Regenerate B05 blocks using new prompt
- Extract V's items with urgency signals (LLM)
- Deduplicate semantically (LLM)
- Stage items via task-system stage.py

### Must Not Touch
- Prompt files (D1.1 handles that)
- Task database directly (only use stage.py)
- Any meetings older than 7 days

## MUST DO

### Step 1: Find Transcripts
```bash
find /home/workspace/Personal/Meetings/Week-of-2026-01-* -name "B01_TRANSCRIPT.md" -mtime -7
```

### Step 2: For Each Transcript — Regenerate B05 (LLM)

Use `/zo/ask` API to call LLM with the new B05 prompt:

```python
import requests, os

def regenerate_b05(transcript_content, meeting_id):
    prompt = f'''You are generating a B05_ACTION_ITEMS block.

Read this transcript and extract action items with decomposition metadata.

## Output Format

```markdown
---
created: 2026-01-26
last_edited: 2026-01-26
version: 1.0
provenance: {conversation_id}
block_id: B05
block_name: ACTION_ITEMS
meeting_id: {meeting_id}
---

## Action Items

### 1. [Action item title]
- **Owner**: [Name]
- **Priority**: [external|strategic|urgent|normal]
- **First Step**: [Specific 2-minute actionable step]
- **Estimated**: [X min]
- **Domain**: [Careerspan|Zo|Personal|Other]
- **Project**: [Inferred project name]
- **Milestones**: 
  1. ...
```

## Transcript

{transcript_content}

Generate B05_ACTION_ITEMS now.'''

    response = requests.post(
        "https://api.zo.computer/zo/ask",
        headers={
            "authorization": os.environ["ZO_CLIENT_IDENTITY_TOKEN"],
            "content-type": "application/json"
        },
        json={"input": prompt}
    )
    return response.json()["output"]
```

### Step 3: Extract V's Items with Urgency Signals (LLM)

For each regenerated B05, call LLM to extract V's items:

```python
def extract_v_items(b05_content, meeting_id):
    prompt = f'''Extract Vrijen's action items from this B05 block.

For each item, assess urgency signals:
- Was this mentioned multiple times in the meeting?
- Did someone remind V about this?
- Is there an explicit deadline?
- Is this an external commitment (investor, partner, customer)?

## Output Format (JSON)

```json
[
  {{
    "title": "Action item title",
    "owner": "Vrijen",
    "priority": "external|strategic|urgent|normal",
    "first_step": "Specific 2-minute action",
    "estimate_min": 15,
    "domain": "Careerspan",
    "project": "Project name",
    "source_meeting": "{meeting_id}",
    "urgency_signals": {{
      "multiple_mentions": false,
      "reminded_by_others": false,
      "explicit_deadline": "2026-01-30 or null",
      "external_commitment": true
    }},
    "context": "Brief context"
  }}
]
```

## B05 Content

{b05_content}

Extract V's items now. Return ONLY valid JSON array.'''

    response = requests.post(
        "https://api.zo.computer/zo/ask",
        headers={
            "authorization": os.environ["ZO_CLIENT_IDENTITY_TOKEN"],
            "content-type": "application/json"
        },
        json={
            "input": prompt,
            "output_format": {
                "type": "array",
                "items": {"type": "object"}
            }
        }
    )
    return response.json()["output"]
```

### Step 4: Deduplicate Semantically (LLM)

After collecting all items, call LLM to dedupe:

```python
def deduplicate_items(all_items):
    prompt = f'''Deduplicate these action items semantically.

Rules:
- Group items that refer to the SAME underlying task (even if worded differently)
- When merging, keep: highest urgency, all source meetings, most specific first_step
- Do NOT merge items that are related but distinct (e.g., "email X" and "call X" are different)

## Items

{json.dumps(all_items, indent=2)}

## Output Format (JSON)

Return deduplicated array. For merged items, update:
- `source_meetings`: [list of all meeting IDs]
- `merged_from_count`: N

```json
[
  {{
    "title": "...",
    "source_meetings": ["meeting1", "meeting2"],
    "merged_from_count": 2,
    ...rest of fields...
  }}
]
```

Deduplicate now. Return ONLY valid JSON array.'''

    response = requests.post(...)
    return response.json()["output"]
```

### Step 5: Stage All Items

```bash
python3 Skills/task-system/scripts/stage.py add \
  --title "TITLE" \
  --priority PRIORITY \
  --estimate ESTIMATE \
  --source "meeting:SOURCE" \
  --context "CONTEXT" \
  --first-step "FIRST_STEP"
```

## MUST NOT DO

- Use regex to parse action items
- Use Python string matching for deduplication
- Directly insert into task database (only stage.py)
- Process meetings older than 7 days
- Touch B05 prompt files (D1.1's job)

## Expected Output

Deposit with:
```json
{
  "status": "complete",
  "transcripts_found": 12,
  "b05_regenerated": 12,
  "items_extracted": 28,
  "items_after_dedup": 18,
  "items_staged": 18,
  "extraction_approach": "LLM semantic via /zo/ask",
  "notes_for_orchestrator": "All items staged, ready for D2.1 interactive review"
}
```

Write extraction_results.json to conversation workspace for D2.1 reference.
