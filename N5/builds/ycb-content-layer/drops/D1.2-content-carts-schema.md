---
drop_id: D1.2
build_slug: ycb-content-layer
wave: W1
stream: 2
order: 1
---

# D1.2: Content Carts Schema

## Objective
Create the database schema for Content Carts - collections of entries for synthesis workflows.

## Context
Content Carts are temporary (or persistent) collections of entries that you gather for a specific purpose: research, synthesis, comparison, etc. Think of them like shopping carts for knowledge.

## Schema Design

### content_carts
```sql
CREATE TABLE content_carts (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT,
    status TEXT DEFAULT 'active',  -- 'active', 'archived', 'deleted'
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

### cart_items
```sql
CREATE TABLE cart_items (
    id TEXT PRIMARY KEY,
    cart_id TEXT NOT NULL,
    item_id TEXT NOT NULL,
    added_at TEXT NOT NULL DEFAULT (datetime('now')),
    notes TEXT,  -- Optional notes about why this item is in the cart
    order_index INTEGER DEFAULT 0,  -- For manual ordering
    FOREIGN KEY (cart_id) REFERENCES content_carts(id) ON DELETE CASCADE,
    FOREIGN KEY (item_id) REFERENCES items(id),
    UNIQUE(cart_id, item_id)
);

CREATE INDEX idx_cart_items_cart ON cart_items(cart_id);
CREATE INDEX idx_cart_items_item ON cart_items(item_id);
```

## Deliverables
1. Migration script at `N5/scripts/migrations/ycb_carts_schema.sql`
2. Python helper functions at `N5/scripts/content_carts.py`:
   - `create_cart(name, description=None)` → cart_id
   - `add_to_cart(cart_id, item_id, notes=None)` → bool
   - `remove_from_cart(cart_id, item_id)` → bool
   - `get_cart(cart_id)` → cart with items
   - `list_carts()` → all carts
   - `archive_cart(cart_id)` → bool

## Success Criteria
- [ ] Migration script runs without errors
- [ ] All tables created with proper indexes
- [ ] Helper functions tested with sample data
- [ ] Cart with items can be retrieved as a list

## Deposit Location
`N5/builds/ycb-content-layer/deposits/D1.2.json`


---

## ⚠️ Retry Context (Attempt 2)

**Previous attempt failed because:** Spawn error on first attempt

**What to do differently:**
- Address the issue described above
- Review the archived deposit to understand what went wrong
- Follow the brief more carefully

