---
drop_id: D3.1
build_slug: pulse-v3
title: Documentation + Config
stream: 3
depends_on: ["D2.1", "D2.2"]
spawn_mode: auto
---

# D3.1: Documentation + Config

## Objective
Update SKILL.md, config, and guidelines to reflect Pulse v3 lifecycle automation capabilities.

## Scope

### Files to UPDATE
- `Skills/pulse/SKILL.md` — Add v3 lifecycle section
- `Skills/pulse/config/pulse_v2_config.json` — Add lifecycle settings
- `N5/prefs/operations/pulse_v2_guidelines.md` — Add v3 section

### Must NOT Touch
- Any Python scripts (already updated by other Drops)
- Build folder structure

## Requirements

### Changes to SKILL.md

Add after the existing "## Pulse v2 Features" section:

```markdown
## Pulse v3 Features (Lifecycle Automation)

### Automated Lifecycle
Pulse v3 automates the entire pre-build pipeline:

```
queued → interviewing → seeded → planning → plan_review → building → tidying → complete
```

The **Lifecycle Agent** polls every 5 minutes and advances tasks automatically:

| From State | To State | Trigger |
|------------|----------|---------|
| queued | interviewing | Automatic on task creation |
| interviewing | seeded | Seeded judgment confidence ≥ 0.8 |
| seeded | planning | Plan generated |
| planning | plan_review | V available (respects calendar/quiet hours) |
| approved | building | Automatic |

### Lifecycle Commands

```bash
# Start lifecycle automation
python3 Skills/pulse/scripts/pulse.py lifecycle start

# Stop lifecycle automation  
python3 Skills/pulse/scripts/pulse.py lifecycle stop

# Check lifecycle status
python3 Skills/pulse/scripts/pulse.py lifecycle status

# Manual single tick (useful for testing)
python3 Skills/pulse/scripts/pulse.py lifecycle tick
python3 Skills/pulse/scripts/pulse.py lifecycle tick --dry-run
```

### Fragment Tagging

Tag SMS responses to route to specific interviews:

```
#task-my-build This is my response about the feature requirements
```

If you have one open interview, responses auto-route. With multiple open interviews, you'll be prompted to use a tag.

### Auto-Fix Dispatch

Tidying findings with confidence ≥ 0.9 auto-fix. Lower confidence findings escalate for manual review.

Configure in `pulse_v2_config.json`:
```json
{
  "auto_fix": {
    "enabled": true,
    "confidence_threshold": 0.9
  }
}
```

### Availability-Aware Reviews

HITL plan reviews respect V's availability:
- Checks Google Calendar for meetings
- Respects quiet hours (configurable, default 10pm-7am ET)
- Looks for `[DW]` (Deep Work) markers in calendar events
- Defers review until next available window

Force review regardless of availability:
```bash
python3 N5/pulse/review_manager.py initiate my-build --force
```
```

### Changes to pulse_v2_config.json

Add these new sections (merge with existing):

```json
{
  "version": "3.0",
  "lifecycle": {
    "enabled": true,
    "poll_interval_minutes": 5,
    "seeded_threshold": 0.8,
    "max_seeded_attempts": 10,
    "idle_shutdown_minutes": 30
  },
  "auto_fix": {
    "enabled": true,
    "confidence_threshold": 0.9
  },
  "fragment_routing": {
    "require_tag_for_multiple": true,
    "auto_route_single_interview": true
  }
}
```

### Changes to pulse_v2_guidelines.md

Add this section after the existing content:

```markdown
## Pulse v3 Additions

### Lifecycle Automation
- Lifecycle agent polls every 5 min when tasks exist in queue
- Auto-advances: queued → interviewing → seeded → planning → plan_review
- Respects availability before HITL gates
- Self-destructs after 30 min of empty queue

### Fragment Tagging
- SMS responses route via `#task-<slug>` prefix
- Single open interview: auto-routes without tag
- Multiple open: prompts for tag

### Auto-Fix
- Tidying findings with confidence ≥ 0.9 auto-fix
- Lower confidence escalates to V
- Configurable threshold in config

### Commands Reference
| Command | Purpose |
|---------|---------|
| `pulse.py lifecycle start` | Create lifecycle agent |
| `pulse.py lifecycle stop` | Delete lifecycle agent |
| `pulse.py lifecycle status` | Show queue state |
| `pulse.py lifecycle tick` | Manual advancement |
| `#task-<slug> <message>` | Route SMS to interview |
```

## Expected Output
- SKILL.md has v3 section with lifecycle docs
- Config includes lifecycle, auto_fix, fragment_routing sections
- Guidelines document v3 additions

## Verification Commands
```bash
# Check SKILL.md contains v3 section
grep -c "v3\|lifecycle\|Lifecycle" Skills/pulse/SKILL.md

# Validate config JSON
python3 -c "import json; json.load(open('Skills/pulse/config/pulse_v2_config.json'))"

# Check guidelines updated
grep -c "v3\|Lifecycle" N5/prefs/operations/pulse_v2_guidelines.md
```

## MUST DO
- Add v3 lifecycle section to SKILL.md
- Add lifecycle, auto_fix, fragment_routing to config
- Add v3 section to guidelines
- Ensure config remains valid JSON
- Update version to "3.0" in config

## MUST NOT DO
- Do not modify any Python scripts
- Do not delete existing documentation
- Do not change config structure for existing v2 fields
