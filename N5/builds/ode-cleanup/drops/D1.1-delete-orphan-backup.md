---
drop_id: D1.1
build_slug: ode-cleanup
name: Delete orphan + backup local export
stream: 1
depends_on: []
spawn_mode: auto
---

# D1.1: Delete Orphan Folder + Create Backup

## Context

The N5OS Ode repo has three copies in the workspace:
1. **GitHub** (remote) — 184 files, 1 commit (force-pushed today, has Skills/ content)
2. **Local Export** (`N5/export/n5os-ode/`) — 160 files, 36 commits (full history, missing new Skills/)
3. **Orphan** (`/home/workspace/n5os-ode/`) — 17 files, NOT a git repo (stale fragment)

You are responsible for filesystem cleanup ONLY — no git operations.

## Scope

### MUST DO
1. List contents of orphan folder `/home/workspace/n5os-ode/` for audit trail
2. Create backup of local export: `cp -r /home/workspace/N5/export/n5os-ode /home/workspace/N5/export/n5os-ode-backup-pre-merge`
3. Delete orphan folder: `rm -rf /home/workspace/n5os-ode`
4. Verify orphan is gone and backup exists

### MUST NOT DO
- Do NOT touch `N5/export/n5os-ode/` (that's for D1.2)
- Do NOT perform any git operations
- Do NOT touch GitHub

### Files You Own
- DELETE: `/home/workspace/n5os-ode/` (entire directory)
- CREATE: `/home/workspace/N5/export/n5os-ode-backup-pre-merge/`

## Expected Output

Write deposit to `N5/builds/ode-cleanup/deposits/D1.1.json`:

```json
{
  "drop_id": "D1.1",
  "status": "complete",
  "completed_at": "<ISO timestamp>",
  "summary": "<1-2 sentence summary>",
  "artifacts": [
    "/home/workspace/N5/export/n5os-ode-backup-pre-merge/"
  ],
  "verification": {
    "orphan_deleted": true,
    "backup_exists": true,
    "backup_file_count": <number>
  },
  "learnings": ["<any learnings for future Drops>"]
}
```

## Verification Commands

```bash
# Orphan is gone
test ! -d /home/workspace/n5os-ode && echo "PASS: Orphan deleted" || echo "FAIL"

# Backup exists
test -d /home/workspace/N5/export/n5os-ode-backup-pre-merge && echo "PASS: Backup exists" || echo "FAIL"

# Backup has content
find /home/workspace/N5/export/n5os-ode-backup-pre-merge -type f | wc -l
```
