---
created: 2026-02-09
last_edited: 2026-02-09
version: 1.0
provenance: con_TS42RN2pwOnBA4Kq
---

# Worker W1.1 — Rate-Limit Hardened Backfill

## Scope
- `N5/cognition/entity_extractor.py` — UPDATE to detect 429s and fallback cleanly.
- `N5/scripts/graph_backfill_continuous.py` — UPDATE to add exponential backoff, batch progress commits, and respect control metadata.
- `N5/cognition/backfill_progress.json` → migrate to SQLite tables `backfill_progress`/`backfill_meta`.
- `N5/config/backfill_control.json` — UPDATE to record run metadata and connect to the scheduled agent.
- Logging/monitoring to confirm `last_run_at` and `status` become accurate.

## Responsibilities
1. Harden the OpenAI extractor to catch truncated JSON + rate-limit responses, forcing `/zo/ask` fallback.
2. Replace the heavyweight JSON list with a durable SQLite progress table.
3. Add an exponential backoff loop and status metadata so the agent knows when we are paused or failed.
4. Verify the scheduled agent actually runs the script and updates control file.

## Must Not Touch
- Do not modify the unified schema in `graph_store.py` (that belongs to W1.2).
- Avoid editing meeting edge review scripts (W2.1 scope).

## Must Do
- Build consistent logging that records `last_rate_limit_ts`, `retry_delay`, and `errors` in `backfill_control.json`.
- Provide CLI/test script to simulate rate limits (mock `OpenAI` response) so we can confirm fallback.

## Output
- Updated extractor/backfill scripts with tests showing resilience.
- New SQLite progress tables + updated control JSON with consistent statuses.
- Documentation of how to simulate rate limits locally.
