---
created: 2026-02-09
last_edited: 2026-02-09
version: 1.0
provenance: con_TS42RN2pwOnBA4Kq
---

# Worker W1.2 — Schema & GraphStore Unification

## Scope
- `N5/cognition/graph_store.py` — EXTEND to support the merged schema (entities + relationships/edges) with provenance metadata.
- New `N5/data/knowledge_graph.db` (or repurposed brain.db) housing the unified tables.
- Migration script (new or existing) that copies `brain.db` entities/relationships and `edges.db` edges into the unified store.
- Query helpers used by other workers (`edge_writer`, `edge_reviewer`, backfill) that rely on the same GraphStore API.

## Responsibilities
1. Define the unified schema with fields for entity type, canonical slug, evidence, meeting provenance, and lifecycle status.
2. Implement GraphStore helpers (`ensure_entity`, `link_relationship`, `record_edge`, `query_edges`) that operate on the unified schema.
3. Create/extend a migration script to move data from `brain.db` + `edges.db` into the new store without loss.
4. Share slug-generation functions to avoid duplicates when meeting edges and block extractions collide.

## Must Not Touch
- Do not modify the rate-limit/backoff logic (W1.1) or review queue automation (W2.1).
- Avoid touching files that only W2.1 uses (`edge_reviewer.py` autop-commit logic) except where required by the shared GraphStore API.

## Must Do
- Provide CLI examples showing how to fetch edges spanning both meeting provenance + general relationships.
- Document migration steps and expected row counts so Validator can confirm success.

## Output
- Unified schema implemented in `knowledge_graph.db`.
- GraphStore library refactored so every ingestion path uses the same helpers.
- Migration script run once; row counts logged for verification.
- Edge query helper ready for W2.1 to plug into.
