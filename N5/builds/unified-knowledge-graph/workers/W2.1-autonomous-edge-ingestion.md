---
created: 2026-02-09
last_edited: 2026-02-09
version: 1.0
provenance: con_TS42RN2pwOnBA4Kq
---

# Worker W2.1 — Autonomous Edge Ingestion

## Scope
- `N5/review/edges/pending/` directory — CLEANUP existing JSONL files, archiving processed batches.
- `N5/scripts/edge_writer.py` (new) — INSERT edges into the unified store with evidence/status.
- `N5/scripts/edge_extractor.py` & `edge_reviewer.py` — UPDATE to use GraphStore helpers and autop-commit.
- Scheduled agent configuration (new or updated) for the ingestion pipeline.

## Responsibilities
1. Drain the 5,442 pending edges: validate each JSON object, insert good ones, log or tag failures.
2. Update the extraction pipeline so new meetings write edges directly to the unified schema instead of staging JSONL, while still tagging each edge with `meeting_id` + `evidence`.
3. Ensure autop-commit logic records `edge_reviewed_at` so quality issues can be triaged.
4. Register a scheduled agent (or service) to run the ingestion script post-meeting and keep `backfill_control.json` in sync with new ingestion state.

## Must Not Touch
- Do not change the unified schema (W1.2) or backfill resilience logic (W1.1).
- Avoid altering `edge_query.py` beyond GraphStore helper usage — updates belong to W1.2.

## Must Do
- Provide a batch log showing how many edges were committed vs skipped.
- Leave markers for edges that failed validation (missing evidence or relation) so humans can review later.
- Ensure the scheduled agent updates control data (status, last run, errors) shared with W1.1.

## Output
- Pending edge backlog cleared; archive + log produced.
- Meeting ingestion pipeline writes to unified graph with autop-commit and review metadata.
- Agent running the ingestion pipeline and updating status fields.
