---
drop_id: D1.2
build_slug: hotline-data-integrity-upgrade
wave: W1
depends_on: []
created: 2026-02-24
spawn_mode: claude_code
spec_completeness: full
---

# D1.2: Dashboard queries canonical ET rollups

## Mission

Update analytics/dashboard query paths to use canonical hotline datasets and ET-day rollups, with explicit exclusion of stale `vapi-calls` from default aggregates.

## Context

Primary canonical datasets:
- `Datasets/zo-hotline-calls/data.duckdb`
- `Datasets/career-hotline-calls/data.duckdb`
- `Datasets/vibe-pill-calls/data.duckdb`
Non-canonical for default dashboards:
- `Datasets/vapi-calls/data.duckdb` (stale archival)

Known discrepancy source: UTC date aggregation vs ET date aggregation. Default reporting should be ET.

## Scenarios

S1: Daily rollup in ET
Given a call at `2026-02-23 02:30:00 UTC`
When dashboard rollup runs
Then day bucket maps to `2026-02-22` ET
Verify rollup query uses ET-adjusted date expression.

S2: Canonical aggregate source
Given all hotline datasets exist including `vapi-calls`
When aggregate totals are computed for dashboard views
Then only canonical three datasets are included
Verify source list in query helper and output metadata indicates canonical datasets.

S3: Empty dataset safety
Given `vibe-pill-calls.calls` has zero rows
When rollup runs
Then output still returns deterministic zero counts without errors
Verify query path handles empty tables and no null-pointer behavior.

## Affected Files

- `Skills/zoren-hotline/scripts/call-logger.ts`
- `Skills/zo-hotline/scripts/call-logger.ts`
- `N5/builds/hotline-data-integrity-upgrade/artifacts/hotline-dashboard-queries.sql` (canonical query reference)
- `N5/builds/hotline-data-integrity-upgrade/artifacts/hotline-query-notes.md`

## Success Criteria

- [ ] Dashboard-facing query helpers return ET-correct rollups.
- [ ] Canonical source usage is explicit and documented.
- [ ] Query outputs are stable when one dataset is empty.

## Deposit Instructions

When complete, write deposit:
```
python3 Skills/pulse/scripts/pulse_cc.py deposit hotline-data-integrity-upgrade D1.2 --status complete --summary "What was done"
```
